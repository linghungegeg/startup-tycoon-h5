# 真实创业模拟经营 H5 网游完整开发计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从 0 开发一款竖版 H5 网游付费游戏，核心是现代真实创业模拟经营、文字事件决策、主线/支线任务、平台币商业化、VIP、排行榜、称号、多服和跨服。

**Architecture:** 前端采用竖版商业手游布局，参考《少年三国志》的顶部资源栏、中部主场景、左右悬浮入口和底部导航。后端负责所有经营结算、平台币、VIP、奖励、排行榜、任务和跨服数据，客户端只负责展示和交互。

**Tech Stack:** React + TypeScript + Vite, Node.js + NestJS, React Admin, MySQL, Redis, BullMQ, Prisma。

---

## 1. 产品定位

这是一款 H5 网游付费游戏，不是纯文字小说，也不是纯战斗游戏。玩家扮演创业公司创始人，从小团队起步，经历招人、接项目、做产品、管现金流、融资、贷款、负债、裁员、竞品竞争、行业风口、政策变化和经营危机，最终走向上市、并购、行业独角兽，或在破产重组后二次创业。

核心卖点：

- 真实创业，不只点升级。
- 文字事件以现代商业信息流呈现。
- 经营压力可翻盘，不轻易 Game Over。
- 平台币商业化、VIP、排行榜、称号完整联动。
- 多服与跨服提供长期竞争目标。

全量托管原则：

- 开发计划、玩法系统、交互体验、任务内容、活动运营、员工体系、经济系统、付费设计均由本计划统一托管。
- 所有新增系统必须先有设计文档、配置结构、验收标准和测试点，再进入实现。
- 详细设计拆分到独立文档，避免总计划过长并降低后续多会话遗漏风险。

配套设计文档：

- `docs/employee-design.md`：员工系统、虚构员工、员工事件、员工付费边界。
- `docs/monetization-design.md`：平台币、VIP、月卡、基金、通行证、礼包、顾问、保险和付费节奏。
- `docs/retention-design.md`：首日、3 日、7 日、14 日、30 日留存目标和主线完成后长期循环。
- `docs/activity-design.md`：赛季、限时活动、活动临时榜、活动通行证、活动商店、经营残局。

## 2. 竖版布局

固定采用《少年三国志》式竖版商业手游结构。

已确认界面方向采用“稳重运营后台 + 手游式玩家入口”方案：

- 玩家端必须先进入独立登录/注册界面，登录后再选区、选头像、命名和进入游戏主界面。
- 玩家端登录入口要像竖版 H5 手游启动页，突出游戏名、账号登录、进入游戏和区服流程，不做普通网页表单堆砌。
- Admin 必须先进入独立后台登录界面，登录后才能进入管理后台。
- Admin 登录后采用稳重运营后台结构：左侧菜单、顶部用户/操作栏、筛选区、表格/列表、分页、批量操作、二次确认和审计入口。
- Admin 不采用卡片模块化堆积作为主要信息结构；概览块只能作为辅助摘要，核心管理页面必须以表格、筛选、详情抽屉/弹窗和操作记录为主。
- 前台和后台都必须使用符合本地用户习惯的中文文案、按钮、错误提示和状态提示。

- 顶部：公司等级、现金、平台币、行动力、负债预警、经营提醒。
- 中部：公司办公室、联合办公空间、独立办公楼、城市商业区或上市公司总部。
- 中部状态：现金流、月收入、月支出、主线任务、待处理事件。
- 左侧入口：财务、融资、贷款。
- 右侧入口：活动、排行、商业、特权、通行证、专属经理。
- 底部导航：公司、员工、业务、市场、商会、背包。

### 2.1 主页入口归属规则

- 主页左右入口只放一级高频入口，不放具体系统子功能。
- 左侧固定承载高频经营主入口：财务、融资、贷款。
- 顶部资源栏只展示现金、平台币、声望、行动力和公司估值，不承担弹窗或页面跳转。
- 右侧固定承载运营活动、排行、商业商城、特权付费、赛季通行证和专属经理提醒入口：活动、排行、商业、特权、通行证、专属经理；右侧使用纵向紧凑入口，必须避开主线任务条和底部导航。
- 商业入口承载普通钱包/商城，集中展示余额、首充、礼包、猎头、保险和普通平台币商品。
- 特权入口承载月卡和成长基金；通行证入口承载赛季通行证购买、状态和奖励线索。
- 头像承载玩家身份和 VIP 创业权益入口；VIP 每日礼包可领或权益待处理时在头像处显示红点，不再占用右侧悬浮入口。
- 风险、合同、用工、财报和舆情提醒归入专属经理提醒流，不在主页左右入口拆成多个独立按钮。
- 邮件只表达系统邮件、奖励邮件和补偿邮件；真实邮件收发/补偿领取页完成前，不作为主页快捷入口。
- 底部导航承载长期主系统：公司、员工、业务、市场、商会、背包。
- 业务入口承载项目交付与产品研发两个标签；二者只合并前台入口，不合并后端系统、数据模型或结算逻辑。
- 背包承载道具、礼包、招募券、活动材料和补偿奖励；产品研发归业务，商战竞争和市场营销归市场；投资合作归融资；企业并购和扩建归公司或主线事件。
- 出门谈判归主线、事件或行动力内页，不做主页大占位。
- 主线任务条属于中部状态，只显示标题、进度和前往，不遮挡左右入口和底部导航。

### 2.2 前台 UI 风格基线

- 后续前台页面默认沿用当前主页 HTML 风格，不重新发明一套视觉语言。
- 统一使用当前主页的深色商务手游风格：深色背景、金色主强调、玻璃面板、圆角图标按钮、底部导航、左右悬浮入口、紧凑资源栏。
- 新增内页、弹窗、活动页、商城页、背包页、排行榜、任务页和事件页，必须优先复用当前主页已有组件形态：`glass-panel`、`btn-gold`、`resource-tag`、顶部资源栏、底部导航、原生 SVG 图标风格。
- 主页内触发的财务、知识/合规支线和规则说明统一使用全覆盖内页；后续不再新增半屏弹窗或居中浮层承载核心经营信息。
- 员工、项目、产品、市场、融资、贷款、任务、专属经理等主系统页统一使用全覆盖内页壳，内部页头、摘要、列表、详情和 CTA 必须向财务、活动、商业等 native 页面风格收敛。
- 不允许为单个功能临时引入不一致的配色、字体、按钮形态、卡片圆角、阴影、营销页式 hero 或普通网页后台风格。
- 如果外部 AI 产出 HTML 设计稿，必须先对照当前主页风格做收敛：保留可用布局和动效，配色、按钮、入口密度、面板质感必须向主页基线靠齐。
- 只有登录/注册、选角、强底图活动页这类有独立底图的页面，可以在不破坏整体游戏气质的前提下做局部差异。

文字事件不以大段小说呈现，而是通过员工私信、客户邮件、投资人聊天、银行提醒、商业新闻、财报预警、会议纪要、合同审批和舆情热搜进入。

## 3. 核心系统设计

### 3.1 主线任务

主线任务负责推进公司阶段、解锁系统和承接 7 日体验。

- 草创阶段：注册公司、选择赛道、招第一名员工、接第一单。
- 生存阶段：完成 3 个项目、现金流转正、查看首次财报。
- 产品阶段：立项 MVP、上线第一个产品、获得第一批用户。
- 资本阶段：申请贷款、完成融资或选择稳健盈利路线。
- 竞争阶段：遭遇竞品、处理价格战、员工被挖、舆情事件。
- 危机阶段：现金流断裂、负债过高、融资失败、债务重组。
- 扩张阶段：进入新城市、新赛道、建立商会合作。
- 成熟阶段：上市、并购、跨服创业大赛、行业第一。

### 3.2 每日任务

每日任务用于留存和轻量循环。

- 登录。
- 完成项目。
- 培训员工。
- 处理事件。
- 查看财报。
- 领取经营收益。
- 参与活动。
- 使用一次市场分析或风险预警。

每日任务按服务器日刷新，奖励不可重复领取。

### 3.3 支线任务

支线按真实商业关系展开。

- 员工线：核心员工成长、离职、加薪、股权激励、被竞品挖角。
- 客户线：大客户压价、延期付款、合同纠纷、长期合作。
- 投资人线：估值谈判、对赌协议、董事会压力、融资失败。
- 银行线：贷款审批、信用评级、逾期催收、债务重组。
- 产品线：MVP、爆款增长、技术债、用户流失、重构。
- 竞品线：价格战、抄袭、挖人、舆论战、专利诉讼。
- 合规线：税务、劳动仲裁、数据合规、广告合规。
- 创始人线：声望、人脉、抗压、行业大会、媒体采访。

### 3.4 公司经营

核心指标：

- 现金。
- 月收入。
- 月支出。
- 净现金流。
- 公司估值。
- 股权比例。
- 负债总额。
- 负债率。
- 信用评级。
- 品牌声誉。
- 员工满意度。
- 客户满意度。
- 市场份额。

财务报表：

- 现金流表。
- 利润表。
- 资产负债表。
- 月度经营报告。

现金不足时进入危机状态，不直接结束游戏。

### 3.5 员工系统

员工字段：

- 头像、职业徽章、岗位、职级、稀有度。
- 技术、销售、运营、管理、创意、财务、法务。
- 薪资、压力、忠诚度、成长潜力、离职风险。

员工操作：

- 招募。
- 培训。
- 晋升。
- 任命。
- 裁员。
- 股权激励。

职级：

- 实习生。
- 初级。
- 中级。
- 高级。
- 专家。
- 总监。
- 合伙人。

### 3.6 项目系统

项目类型：

- 外包开发。
- 企业咨询。
- SaaS 定制。
- 营销增长。
- 数据分析。
- AI 自动化方案。

项目有周期、预算、风险、成功率、员工分配、延期、失败、结算和客户满意度。项目结算必须幂等，不能重复发奖。

### 3.7 产品生命周期

产品阶段：

- 立项。
- MVP。
- 内测。
- 上线。
- 增长期。
- 成熟期。
- 衰退期。
- 重构。
- 关闭。

产品指标：

- 用户数。
- 留存率。
- 付费率。
- 获客成本。
- 服务器成本。
- 口碑。
- 技术债。

产品能产生长期收入，但增长会带来服务器、客服、合规和技术债压力。

### 3.8 贷款、融资与负债

贷款类型：

- 短期周转贷。
- 银行经营贷。
- 设备贷款。
- 高息应急贷。
- 政策扶持贷款。

融资轮次：

- 种子轮。
- 天使轮。
- A 轮。
- B 轮。
- C 轮。
- IPO。
- 并购。

负债后路线：

- 正常还款。
- 提前还款。
- 借新还旧。
- 裁员降本。
- 出售资产。
- 引入融资。
- 债务重组。
- 破产保护剧情线。

融资获得现金但稀释股权；贷款不稀释股权但增加负债和还款压力。

### 3.9 市场竞争与赛道差异

创业赛道：

- AI Agent 企业服务。
- 机器人/具身智能。
- 低空经济。
- 跨境 SaaS。
- 直播电商工具。
- 新能源运营服务。
- 银发经济服务。

竞品行为：

- 价格战。
- 抢客户。
- 挖员工。
- 舆论战。
- 专利诉讼。
- 抢融资。
- 产品抄袭。

不同赛道必须有不同成本结构、事件、政策风险、人才压力和增长路径。

### 3.10 现代化文字事件

事件入口：

- 员工私信。
- 客户邮件。
- 投资人聊天。
- 银行还款提醒。
- 商业新闻。
- 财报预警。
- 会议纪要。
- 合同审批。
- 舆情热搜。

每个事件提供 2-4 个决策卡，展示收益、成本、风险、影响指标和后续可能事件。选择结果必须落库，并能推进主线、支线或后续事件链。

### 3.11 平台币与商城

第一版主要以平台币支付为主，外部支付接口只做预留。

规则：

- 游戏内商品统一消耗平台币。
- 平台币余额只以后端为准。
- 平台币可由 Admin 后台发放、扣减、修正。
- 平台币变更必须写流水。
- 平台币购买商品必须在同一事务内完成扣减和发货。
- 平台币不足时购买失败且不发货。
- 重复请求不能重复扣币或重复发货。
- 外部支付订单、回调、验签、发货、补单流程预留接口和数据模型，但不作为第一版强依赖。

平台币来源：

- 后台发放。
- 活动奖励。
- 系统补偿。
- 预留外部充值接口。

平台币消耗：

- 月卡。
- 成长基金。
- 通行证。
- 限时礼包。
- 猎头招募券。
- VIP 商店。
- 活动商店。
- 经营顾问、风险保险、加速券等道具。

### 3.12 VIP 系统

关键规则：

- 玩家在游戏内消耗平台币，计入 VIP 经验。
- VIP 经验按消耗平台币数量换算。
- 后台发放平台币不直接增加 VIP 经验。
- 后台可手动调整 VIP 等级或经验，但必须写审计日志。
- VIP 不直接消灭现金流、负债和经营判断，只提供便利、信息优势和轻量加成。

VIP 权益：

- 每日礼包。
- 行动力上限提高。
- 快速项目结算次数增加。
- 员工培训队列增加。
- 免费刷新招聘次数增加。
- 财报高级分析提示。
- 风险预警提前。
- 专属头像框。
- 专属称号。
- 办公室皮肤。
- VIP 商店折扣。
- 专属礼包购买权限。

### 3.13 排行榜系统

第一版排行榜必须收敛，避免榜单过多导致玩家看不懂重点、奖励难平衡、结算复杂度过高。

常驻本服榜：

- 公司估值榜。
- 净现金流榜。
- 产品增长榜。
- 商会榜。

常驻跨服榜：

- 跨服创业大赛榜。
- 跨服商会榜。

活动临时榜：

- AI Agent 风口榜。
- 低空经济增长榜。
- 现金流挑战榜。
- 融资路演榜。
- 产品爆发榜。
- 合规经营挑战榜。
- 知识达人榜。

排行榜入口只显示本服、跨服、活动三类。活动榜只在活动开启时出现，活动结束后隐藏入口，但奖励、称号和成就记录保留。

排行榜奖励通过邮件发放，必须幂等。跨服榜按开服时间和活跃度分池，避免老服碾压新服。

### 3.14 称号系统

称号来源：

- 主线进度。
- 支线成就。
- 排行榜名次。
- 赛季活动。
- VIP 等级。
- 特殊经营结果。

称号类型：

- 成长称号：初创老板、连续创业者、行业新星。
- 财务称号：现金流大师、负债猎手、稳健经营者。
- 融资称号：天使宠儿、资本新贵、独角兽创始人。
- 产品称号：爆款制造机、技术理想派、增长黑客。
- 风险称号：绝境翻盘者、债务重组专家。
- 排名称号：本服首富、跨服独角兽、赛季冠军。
- VIP 称号：尊享会员、战略投资人。

称号可展示在头像、排行榜、商会、聊天和玩家资料。称号只提供轻量加成，同时只能装备 1 个主称号，避免属性堆叠失控。

### 3.15 成就系统

成就系统记录玩家创业履历，作为长期收集、荣誉反馈和知识解锁入口。

成就类型：

- 成长成就：完成第一单、现金流转正、公司估值破千万、上市。
- 财务成就：连续现金流为正、成功债务重组、负债率控制优秀。
- 员工成就：培养总监、留住被挖角员工、完成股权激励。
- 产品成就：上线 MVP、产品用户破万、完成技术债重构。
- 融资成就：完成天使轮、拒绝低估值融资、完成 IPO。
- 合规成就：正确处理劳动纠纷、完成数据合规整改、避免合同违约。
- 知识成就：解锁创业知识、完成合规问答、连续正确处理风险事件。
- 隐藏成就：绝境翻盘、破产重组成功、竞品暴雷后反超。

成就奖励可包含少量平台币、现金、行动力、经营资源、称号、头像框、办公室装饰和知识图鉴条目。成就奖励不直接增加 VIP 经验，VIP 经验仍以游戏内平台币消费为主。

### 3.16 创业知识库

创业知识库用于把真实经营中遇到的法律、合规、财务、用工和商业常识游戏化呈现。知识内容在开发和运营配置阶段通过联网检索官方或权威来源整理为游戏内科普卡，不依赖纯手写猜测。

知识分类：

- 公司设立与股权：注册资本、股东责任、股权稀释、董事会。
- 劳动用工：劳动合同、试用期、加班、裁员、经济补偿。
- 合同与客户：合同违约、付款周期、保密条款、竞业限制。
- 融资与投资：投资条款、对赌、优先清算权、股权激励。
- 贷款与债务：利息、逾期、担保、债务重组。
- 数据与隐私：用户授权、个人信息处理、数据泄露、跨境数据。
- 广告与营销：虚假宣传、广告合规、达人合作风险。
- 知识产权：商标、著作权、专利、软件代码归属。
- 税务与财务：发票、税负、成本入账、财报真实性。

知识内容来源策略：

- 优先检索全国人大网、政府网、国务院部门官网、国家市场监督管理总局、国家税务总局、国家网信办等官方来源。
- 可参考《公司法》《劳动合同法》《民法典合同编》《个人信息保护法》《广告法》《商标法》《著作权法》《税收征收管理法》等公开法律法规。
- 每条知识卡只做通俗科普和游戏风险提示，不直接复制大段法条。
- 每条知识卡必须标明来源名称、来源链接、采集日期和内容版本。
- 所有知识卡固定展示免责声明：仅作游戏科普，不构成法律建议。

首批知识内容种子：

- 公司设立与股权：公司设立、股东出资、股权稀释、董事会责任。来源优先使用全国人大网关于《公司法》修订说明和公开法律文本。
- 劳动用工：书面劳动合同、试用期、薪酬支付、解除劳动关系、经济补偿。来源优先使用全国人大网《劳动合同法》和《劳动法》公开文本。
- 合同与客户：合同订立、违约责任、付款延期、保密条款、合同解除。来源优先使用政府网/全国人大网《民法典》合同编相关公开内容。
- 数据与隐私：个人信息处理、用户同意、敏感个人信息、数据泄露通知。来源优先使用全国人大网《个人信息保护法》公开文本。
- 税务与财务：发票、纳税申报、税收征管、财务真实性。来源优先使用政府网《税收征收管理法》和国家税务总局公开说明。
- 广告与营销：虚假宣传、广告代言、直播/达人合作风险。来源优先使用政府网、市场监管部门公开法规和案例。
- 知识产权：商标、著作权、专利、软件代码归属。来源优先使用国家知识产权局、版权主管部门和公开法律文本。
- 贷款与债务：借款合同、担保、逾期、债务重组。来源优先使用《民法典》合同编及金融监管公开规则。

首批知识卡示例：

- “未签劳动合同的风险”：触发于员工入职后未规范签约事件，解释短期省事会提高劳动争议风险。
- “客户延期付款怎么办”：触发于大客户拖款事件，解释合同约定、催收、违约成本和客户关系取舍。
- “用户数据泄露危机”：触发于产品数据事故，解释及时补救、通知、整改和声誉影响。
- “融资为什么会稀释股权”：触发于天使轮/A 轮融资，解释现金增加与控制权下降的交换。
- “广告宣传不能乱承诺”：触发于营销投放事件，解释夸大宣传可能带来监管和声誉风险。
- “税务合规不是可选项”：触发于财务月报或税务稽查事件，解释现金流压力下也不能靠违规少缴税解决问题。

知识卡结构：

- 现实场景。
- 风险提示。
- 游戏选择。
- 知识解释。
- 来源标记。
- 游戏影响。
- 免责声明。

知识通过事件、任务、成就解锁。事件结算页显示“经营结果”“为什么会这样”“解锁知识”三段反馈。

### 3.17 商会与跨服

商会功能：

- 商会成员。
- 商会任务。
- 商会科技。
- 商会互助。
- 商会排行榜。
- 商会贡献。

跨服功能：

- 跨服分组。
- 跨服创业大赛。
- 跨服排行榜。
- 跨服称号。
- 跨服奖励结算。
- 跨服数据快照。

跨服竞争不以战斗为核心，而是比经营结果：估值、现金流、产品用户数、市场份额、负债控制和活动积分。

## 4. 技术架构

### 4.1 前端

- React + TypeScript + Vite。
- 竖版 H5。
- 优先适配 375x667 到 430x932。
- 图片资源包括头像、员工、职业徽章、产品 Logo、投资机构 Logo、银行 Logo、办公场景。
- 前端只负责展示和交互，不做可信结算。

### 4.2 后端

- Node.js + NestJS。
- MySQL 存账号、角色、公司、员工、项目、产品、任务、事件、财务、平台币、VIP、订单预留、排行榜快照、称号、成就、知识库、商会、后台审计。
- Redis 存会话、排行榜缓存、限流、活动缓存、分布式锁。
- BullMQ 处理邮件、活动结算、跨服榜单、平台币相关异步审计、外部支付预留回调。
- ORM 使用 Prisma，后续阶段不再切换 ORM。

### 4.3 管理后台

- React Admin。
- 后台登录和权限角色。
- 玩家查询、公司财务查询、平台币查询、VIP 查询、称号查询、订单预留查询。
- 平台币发放、扣减、修正。
- VIP 调整。
- 商品、任务、事件、活动、排行榜、称号、成就、知识库配置。
- 邮件补偿、封禁/解封、区服管理、跨服分组。
- 服务器备份、数据库备份、静态资源备份和回滚依赖宝塔面板，不在游戏后台自建备份/回滚系统。
- 所有高风险操作写审计日志。

## 5. 主要 API

- Auth API：注册、登录、刷新 token。
- Server API：区服列表、推荐区服、选服。
- Profile API：头像、命名、创建公司。
- Company API：公司状态、财报、升级。
- Employee API：招募、培训、晋升、任命、裁员。
- Project API：接单、推进、结算。
- Product API：研发、上线、运营。
- Finance API：贷款、还款、融资、负债。
- Task API：主线、每日、支线、领奖。
- Event API：文字事件、选择、结算。
- Wallet API：平台币余额、流水。
- Shop API：商品、平台币购买、购买记录。
- VIP API：等级、经验、权益、礼包。
- Rank API：榜单、我的排名、奖励预览。
- Title API：称号、装备、过期状态。
- Achievement API：成就列表、完成状态、领取奖励、成就详情。
- Knowledge API：知识分类、知识条目、已解锁知识、知识点详情。
- Ops API：后台配置、玩家查询、平台币发放、VIP 调整、邮件补偿。
- Admin Achievement API：成就配置、奖励配置、完成条件配置。
- Admin Knowledge API：知识条目配置、来源链接、采集日期、内容版本、审核状态。
- Cross Server API：跨服排行、跨服活动、奖励结算。

## 6. 数据模型范围

核心表范围：

- 账号表。
- 区服表。
- 角色表。
- 公司表。
- 头像配置表。
- 员工表和员工配置表。
- 项目表和项目配置表。
- 产品表和产品配置表。
- 任务表、任务配置表、任务进度表。
- 事件表、事件配置表、事件选择记录表。
- 财务流水表。
- 贷款表、还款计划表。
- 融资表、股权变动表。
- 平台币账户表。
- 平台币流水表。
- 商品配置表。
- 商品购买记录表。
- 外部支付订单预留表。
- VIP 等级配置表。
- VIP 权益配置表。
- 玩家 VIP 表。
- VIP 经验流水表。
- VIP 权益领取记录表。
- 排行榜配置表。
- 排行榜快照表。
- 排行榜奖励发放表。
- 称号配置表。
- 玩家称号表。
- 称号装备表。
- 成就配置表。
- 玩家成就表。
- 成就奖励领取记录表。
- 知识分类表。
- 知识条目表。
- 玩家知识解锁表。
- 事件知识关联表。
- 知识来源配置表。
- 知识内容审核记录表。
- 商会表、商会成员表、商会贡献表。
- 邮件表。
- 后台用户表、后台角色表、后台审计日志表。

## 7. 开发阶段

### Phase 0：工程初始化

目标：建立可持续开发的基础工程。

开发内容：

- [ ] 初始化前端 H5 项目。
- [ ] 初始化后端 API 项目。
- [ ] 初始化管理后台项目。
- [ ] 配置 MySQL、Redis、本地环境变量。
- [ ] 建立基础目录结构。
- [ ] 建立统一代码规范、类型检查、构建命令。
- [ ] 建立 API 返回格式和错误码规范。
- [ ] 建立基础日志和请求追踪。

验收标准：

- 前端可启动并显示空主界面。
- 后端可启动并提供健康检查接口。
- 后台可启动并显示登录占位页。
- MySQL、Redis 连接正常。

阶段检查：

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- 后端健康检查接口测试。
- 数据库连接测试。

### Phase 1：账号、登录、区服、角色创建

目标：完成商业网游基础进入流程。

开发内容：

- [ ] 注册账号。
- [ ] 登录账号。
- [ ] Token 登录态。
- [ ] 区服列表。
- [ ] 推荐新区。
- [ ] 选择区服。
- [ ] 选择头像。
- [ ] 输入创始人名和公司名。
- [ ] 创建公司角色。
- [ ] 同账号多区服角色隔离。

验收标准：

- 用户能完成注册、登录、选服、选头像、命名、创建公司。
- 同一账号可在多个区服创建不同公司。
- 刷新页面后登录态可恢复。
- 已创建角色再次进入对应区服时直接进入主界面。
- 玩家登录/注册界面必须独立于游戏主界面，未登录不得进入选区或主界面。
- 登录、注册、选区、头像、命名和错误提示必须是正式中文产品文案，不允许出现测试文案、调试文案或临时英文文案。

阶段检查：

- 注册登录接口测试。
- Token 过期和刷新测试。
- 多区服角色隔离测试。
- 前端路由守卫测试。
- 入服全流程文案检查。
- `lint/typecheck/build` 全部通过。

### Phase 1.5：前后台登录与布局基线调整

目标：在继续开发经营系统前，先固定前台和 Admin 的正式入口、布局和文案基线，避免后续功能接到错误界面结构上。

开发内容：

- [ ] 玩家端独立登录/注册启动页。
- [ ] 玩家端登录后再进入选区、头像、命名和游戏主界面。
- [ ] 玩家端启动页调整为竖版 H5 手游入口，不做普通网页表单堆砌。
- [ ] Admin 独立登录页。
- [ ] Admin 登录后才能进入后台管理界面。
- [ ] Admin 主界面改为稳重运营后台结构：左侧菜单、顶部栏、筛选区、表格/列表、分页和操作区。
- [ ] 清理测试文案、占位文案、调试文案、临时英文文案和不符合真实创业设定的表达。

验收标准：

- 玩家端未登录不得进入选区或游戏主界面。
- Admin 未登录不得进入后台管理界面。
- 玩家端登录入口具备手游启动页观感，并保留注册/登录、错误提示和 API 联动。
- Admin 不以卡片模块堆积作为主要内容结构。
- Admin 页面核心区域以筛选、表格、状态、操作和分页为主。
- 所有可见文案均为正式中文产品文案。

阶段检查：

- 玩家端注册/登录/选区/创建档案流程检查。
- Admin 登录拦截和退出登录检查。
- Admin 菜单、筛选、表格、分页和操作区展示检查。
- 前台和后台文案正式性检查。
- `lint/typecheck/build` 全部通过。

### Phase 1.6：MySQL/Prisma 数据库基线与本地数据区打通

目标：在继续开发经营系统前，先把本地 MySQL 作为正式数据源接入，账号、后台账号、区服、头像、角色档案不再依赖内存数据，后续每个系统都按数据库优先推进。

开发内容：

- [ ] 选定 Prisma 作为 ORM，并固定后续阶段使用。
- [ ] 建立账号、账号会话、管理员、管理员会话、后台审计、区服、头像、角色档案基础表。
- [ ] 建立本地 MySQL 数据库和 Prisma schema。
- [ ] 建立正式种子数据：区服、头像、初始管理员。
- [ ] 玩家注册、登录、选区、选头像、创建档案全部写入 MySQL。
- [ ] Admin 登录走 MySQL 管理员账号和管理员会话。
- [ ] 玩家端和 Admin 本地缓存只用于恢复入口，进入系统前必须重新通过 API 校验数据库会话。
- [ ] 更新开发计划，明确后续阶段必须先设计数据库结构、迁移/同步和种子数据。

验收标准：

- 本地 MySQL 可通过 Prisma schema 同步。
- 种子数据可重复执行，不重复插入错误数据。
- 玩家账号、角色档案和后台管理员会话都以后端数据库为准。
- 前端不能依赖本地缓存绕过 API 进入正式系统。
- Admin 未通过后端登录不能进入后台。
- 数据库密码、密钥等敏感信息不得写入仓库。

阶段检查：

- `npm run db:generate -w @wenziyouxi/api`
- `npm run db:push -w @wenziyouxi/api`
- `npm run db:seed -w @wenziyouxi/api`
- API 注册、登录、选区、建档接口测试。
- Admin 登录和会话接口测试。
- `lint/typecheck/build/test` 全部通过。

### Phase 2：竖版主界面

目标：完成参考《少年三国志》的竖版商业手游布局。

开发内容：

- [ ] 顶部资源栏。
- [ ] 中部主场景。
- [ ] 中部状态区。
- [ ] 左侧经营入口。
- [ ] 右侧商业化和活动入口。
- [ ] 底部主导航。
- [ ] 移动端适配。
- [ ] 图片占位资源。

验收标准：

- H5 竖屏布局稳定。
- 核心入口不重叠。
- 小屏幕可正常点击。
- 主界面能展示真实玩家公司数据。
- 平台币余额从后端加载，不信任前端缓存。

阶段检查：

- 移动端关键尺寸截图检查。
- 文案溢出检查。
- 资源加载失败兜底检查。
- 平台币余额展示一致性检查。
- `lint/typecheck/build` 全部通过。

### Phase 3：公司经营与财务

目标：建立真实经营核心数据模型。

开发内容：

- [ ] 公司等级。
- [ ] 现金余额。
- [ ] 月收入、月支出、净现金流。
- [ ] 公司估值。
- [ ] 股权比例。
- [ ] 负债总额和负债率。
- [ ] 信用评级。
- [ ] 品牌声誉、员工满意度、客户满意度。
- [ ] 基础财务报表。
- [ ] 每日/月度经营结算。

验收标准：

- 公司状态可从后端加载。
- 财务数据能正确计算。
- 月度经营报告能展示收入、支出、净现金流和风险提示。
- 现金不足时能进入风险状态，不直接 Game Over。

阶段检查：

- 财务计算单元测试。
- 现金流边界测试。
- 负数、零值、大数测试。
- 前后端字段一致性检查。
- `lint/typecheck/build` 全部通过。

### Phase 4：员工系统

目标：完成员工收集、养成和真实组织管理。

开发内容：

- [ ] 员工列表。
- [ ] 员工头像和职业徽章。
- [ ] 岗位、职级、稀有度。
- [ ] 员工属性。
- [ ] 薪资、压力、忠诚度、成长潜力。
- [ ] 招募、培训、晋升、任命、裁员。
- [ ] 股权激励。

验收标准：

- 玩家能招募员工。
- 员工能参与项目或公司加成。
- 员工升级会提升能力，也可能提升薪资压力。
- 裁员会降低支出，但影响士气和声誉。

阶段检查：

- 员工招募概率测试。
- 员工属性成长测试。
- 裁员影响测试。
- 员工数据持久化测试。
- `lint/typecheck/build` 全部通过。

### Phase 5：项目系统

目标：形成第一条完整经营循环。

开发内容：

- [ ] 项目列表、类型、周期、预算、风险、成功率。
- [ ] 项目员工分配。
- [ ] 项目推进、延期、失败、结算。
- [ ] 客户满意度影响。

验收标准：

- 玩家能接项目、派员工、推进、结算。
- 项目成功获得收入。
- 项目失败可能损失声誉、客户满意度或赔偿。
- 项目结算不能重复领取奖励。

阶段检查：

- 项目结算幂等测试。
- 成功率计算测试。
- 员工分配影响测试。
- 刷新/断线后项目状态恢复测试。
- `lint/typecheck/build` 全部通过。

### Phase 6：主线、每日、支线任务

目标：建立完整任务体系。

开发内容：

- [ ] 主线任务配置。
- [ ] 每日任务配置。
- [ ] 支线任务配置。
- [ ] 任务条件检查。
- [ ] 任务奖励领取。
- [ ] 每日刷新。
- [ ] 任务进度持久化。
- [ ] 任务引导入口。
- [ ] 知识任务。
- [ ] 合规支线任务。

验收标准：

- 主线能推动 7 日体验。
- 每日任务按服务器日刷新。
- 支线可按条件触发。
- 奖励不可重复领取。
- 知识任务能引导玩家查看和理解创业知识卡。
- 合规支线能从真实经营事件触发。

阶段检查：

- 任务触发测试。
- 每日刷新测试。
- 重复领奖测试。
- 任务配置合法性检查。
- 知识任务触发测试。
- 合规支线推进测试。
- `lint/typecheck/build` 全部通过。

### Phase 7：现代化文字事件

目标：让文字玩法以现代交互形式呈现。

开发内容：

- [ ] 事件中心。
- [ ] 聊天式事件。
- [ ] 邮件式事件。
- [ ] 商业新闻事件。
- [ ] 财报预警事件。
- [ ] 合同审批事件。
- [ ] 会议纪要事件。
- [ ] 舆情热搜事件。
- [ ] 决策卡片。
- [ ] 事件选择结果结算。
- [ ] 后续事件链。
- [ ] 知识点解锁。
- [ ] 风险解释卡。

验收标准：

- 事件来自消息、邮件、新闻、财报等现代入口。
- 玩家选择后能看到明确经营影响。
- 事件结果能落库。
- 事件能触发后续支线。
- 事件选择能解锁对应知识卡。
- 事件结算能解释经营结果和风险原因。

阶段检查：

- 事件配置解析测试。
- 事件选择结算测试。
- 后续事件触发测试。
- 知识点解锁测试。
- 风险解释卡展示测试。
- 前端长文本适配检查。
- `lint/typecheck/build` 全部通过。

### Phase 8：贷款、负债与危机

目标：把真实经营压力做成核心玩法。

开发内容：

- [ ] 贷款申请、类型、利率、期限、每期还款。
- [ ] 提前还款、逾期、罚息。
- [ ] 信用评级、负债率。
- [ ] 现金流危机。
- [ ] 债务重组。
- [ ] 裁员降本联动。
- [ ] 破产保护剧情线。

验收标准：

- 贷款能解决短期现金流，但增加长期压力。
- 逾期会影响信用和融资条件。
- 高负债会触发风险事件。
- 危机状态至少有 3 种处理路线：融资、降本、重组。

阶段检查：

- 还款计划计算测试。
- 逾期罚息测试。
- 信用评级变化测试。
- 危机触发测试。
- `lint/typecheck/build` 全部通过。

### Phase 9：融资、股权与投资人

目标：实现创业融资的真实取舍。

开发内容：

- [ ] 融资轮次。
- [ ] 投资人列表。
- [ ] 估值计算。
- [ ] 融资金额。
- [ ] 股权稀释。
- [ ] 投资人条款。
- [ ] 融资成功率。
- [ ] 融资失败事件。
- [ ] 董事会压力。
- [ ] 对赌协议。

验收标准：

- 融资能获得现金，但会稀释股权。
- 公司状态影响融资估值。
- 负债过高会降低融资条件。
- 融资失败能触发危机或替代路线。

阶段检查：

- 股权稀释计算测试。
- 估值公式测试。
- 融资条件边界测试。
- 投资人事件测试。
- `lint/typecheck/build` 全部通过。

### Phase 10：产品生命周期

目标：让公司不只靠接项目赚钱。

开发内容：

- [ ] 产品立项、MVP、内测、上线。
- [ ] 增长期、成熟期、衰退期。
- [ ] 产品重构和关闭。
- [ ] 用户数、留存率、付费率。
- [ ] 获客成本、服务器成本、口碑、技术债。

验收标准：

- 产品能产生长期收入。
- 产品增长会带来服务器和客服成本。
- 技术债过高会触发事故。
- 产品衰退需要转型或重构。

阶段检查：

- 产品阶段流转测试。
- 产品收入计算测试。
- 技术债事件测试。
- 产品关闭影响测试。
- `lint/typecheck/build` 全部通过。

### Phase 11：市场竞争与赛道差异

目标：加入真实商业外部压力。

开发内容：

- [ ] 创业赛道。
- [ ] 竞品公司。
- [ ] 行业热度。
- [ ] 政策变化。
- [ ] 价格战。
- [ ] 抢客户。
- [ ] 挖员工。
- [ ] 舆论战。
- [ ] 专利诉讼。
- [ ] 市场份额。

验收标准：

- 不同赛道成本结构不同。
- 不同赛道事件不同。
- 竞品行为能影响玩家经营。
- 玩家有可选应对策略。

阶段检查：

- 赛道配置测试。
- 竞品事件测试。
- 市场份额计算测试。
- 竞品挖人和价格战边界测试。
- `lint/typecheck/build` 全部通过。

### Phase 12：平台币与商城

目标：完成以平台币为核心的商业闭环，并预留外部支付接口。

开发内容：

- [ ] 平台币账户。
- [ ] 平台币余额查询。
- [ ] 平台币流水。
- [ ] Admin 后台发放、扣减、修正平台币。
- [ ] 商品配置。
- [ ] 平台币购买商品。
- [ ] 首充入口占位。
- [ ] 月卡、成长基金、通行证、限时礼包、猎头招募券、活动商店。
- [ ] 外部支付订单模型预留。
- [ ] 外部支付回调接口预留。
- [ ] 外部支付验签扩展位预留。
- [ ] 外部支付发货流程预留。
- [ ] 补单流程预留。

验收标准：

- 玩家能用平台币购买商品。
- 平台币余额以后端为准。
- 平台币扣减和发货必须在同一事务内完成。
- 平台币不足时购买失败且不发货。
- 重复请求不会重复扣币或重复发货。
- Admin 可发放平台币并记录审计日志。
- 外部支付接口有清晰预留，但不影响第一版平台币闭环。

阶段检查：

- 平台币扣减事务测试。
- 平台币流水一致性测试。
- 重复购买幂等测试。
- 后台发币权限测试。
- 后台发币审计日志测试。
- 商品配置测试。
- 外部支付预留接口结构检查。
- `lint/typecheck/build` 全部通过。

### Phase 13：VIP 系统

目标：完成 VIP 经验、等级、权益和平台币消费联动。

开发内容：

- [ ] VIP 等级配置。
- [ ] VIP 经验来源。
- [ ] 平台币消费转 VIP 经验。
- [ ] VIP 权益配置。
- [ ] VIP 每日礼包。
- [ ] VIP 专属称号。
- [ ] VIP 专属头像框。
- [ ] VIP 商店折扣。
- [ ] VIP 权益领取记录。
- [ ] 后台调整 VIP 等级或经验。
- [ ] 后台查询 VIP 记录。
- [ ] 后台配置 VIP 权益。

验收标准：

- 游戏内平台币消费能增加 VIP 经验。
- 后台发放平台币不误加 VIP 经验。
- VIP 升级后权益正确生效。
- VIP 每日礼包不可重复领取。
- VIP 专属称号能获得和展示。
- 后台调整必须写审计日志。

阶段检查：

- VIP 经验计算测试。
- 平台币消费加 VIP 经验测试。
- 后台发币不加 VIP 经验测试。
- VIP 权益生效测试。
- VIP 每日礼包重复领取测试。
- VIP 称号发放测试。
- 后台 VIP 权限测试。
- 后台审计日志测试。
- `lint/typecheck/build` 全部通过。

### Phase 14：排行榜、称号、成就、知识库、商会

目标：完成收敛后的单服长期竞争、身份展示、创业履历和知识收集。

开发内容：

- [x] 常驻本服排行榜：公司估值榜、现金流榜、产品增长榜、商会榜。
- [x] 活动临时榜入口。
- [x] 排行榜快照。
- [x] 排行榜奖励。
- [x] 排行榜邮件发奖。
- [x] 称号配置。
- [x] 称号获得。
- [x] 称号装备。
- [x] 称号过期。
- [x] 称号展示。
- [x] 成就配置。
- [x] 成就条件检查。
- [x] 成就完成记录。
- [x] 成就奖励领取。
- [x] 隐藏成就。
- [x] 知识分类。
- [x] 知识条目。
- [x] 知识解锁记录。
- [x] 事件知识关联。
- [x] 知识卡来源链接、采集日期、内容版本和免责声明。
- [x] 商会。
- [x] 商会成员。
- [x] 商会任务。
- [x] 商会科技。
- [x] 商会互助。
- [x] 商会排行榜。

验收标准：

- 排行榜能按服务器时间结算。
- 常驻本服榜只显示 4 个。
- 活动榜未开启时不显示。
- 活动结束后榜单隐藏，但奖励、称号、成就记录保留。
- 排行榜奖励不会重复发放。
- 玩家能获得、装备、展示称号。
- 限时称号能过期。
- 玩家能完成成就并领取奖励。
- 成就奖励不可重复领取。
- 已解锁知识可在知识库查看。
- 知识卡必须展示来源和“仅作游戏科普，不构成法律建议”。
- 商会基础功能可用。
- 排行榜、称号、成就、商会成员列表展示一致。

阶段总结：

- 当前阶段：Phase 14：排行榜、称号、成就、知识库、商会。
- 完成内容：落地排行榜快照与奖励记录、称号获取/装备/过期、成就进度与幂等奖励、知识卡来源展示、商会加入/互助/成员与贡献榜，并接入主页荣誉中心。
- 四插件使用证据：
  - Superpowers：确认 Phase 14 成功标准、当前稳定点 `c8437d5`、最小改动范围和提交前验证门槛。
  - Browser Use：在 `http://127.0.0.1:5173/` 完成注册/建档、打开排行、领取成就、解锁知识、加入商会和发布互助。
  - Build Web Apps：检查荣誉中心沿用主页深色商务手游基线、金色强调、glass-panel、顶部资源栏、左右入口和底部导航。
  - Game Studio：检查 HUD 不遮挡主流程，排行榜/称号/成就/知识/商会奖励反馈在玩法入口内可读。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`。
- 浏览器/截图验收：Browser Use 截图确认 4 个常驻本服榜、活动榜未开启、成就领取、知识卡免责声明、商会加入和互助贡献更新。
- 遗留风险：活动临时榜当前为未开启状态；后续活动系统接入时需要补充真实活动榜配置和活动结束回收规则。
- 下一阶段入口：Phase 15。
- 是否可作为稳定点：可以。

阶段检查：

- 排行榜缓存测试。
- 排行榜快照测试。
- 排行榜奖励幂等测试。
- 活动榜显示/隐藏测试。
- 称号获得测试。
- 称号过期测试。
- 称号装备位测试。
- 成就触发测试。
- 成就奖励幂等测试。
- 隐藏成就展示测试。
- 知识解锁测试。
- 知识卡来源字段测试。
- 商会权限测试。
- `lint/typecheck/build` 全部通过。

### Phase 15：跨服系统

目标：完成多服商业游戏的中后期竞争。

开发内容：

- [x] 跨服分组。
- [x] 跨服创业大赛。
- [x] 跨服商会榜。
- [x] 跨服奖励结算。
- [x] 跨服称号发放和过期。
- [x] 跨服活动报名。
- [x] 跨服数据快照。

验收标准：

- 跨服活动不破坏单服经济。
- 跨服奖励可重试且幂等。
- 后台可调整跨服分组。
- 新服不会被老服碾压，按开服时间或活跃度分池。
- 跨服称号展示优先级正确。

阶段检查：

- 跨服分组测试。
- 跨服榜单结算测试。
- 跨服奖励重复发放测试。
- 跨服称号发放测试。
- 跨服称号过期测试。
- 跨服数据快照一致性测试。
- `lint/typecheck/build` 全部通过。

阶段总结：

- 当前阶段：Phase 15：跨服系统。
- 完成内容：落地跨服分组、区服入池配置、跨服报名、跨服创业大赛榜、跨服商会榜、跨服数据快照、跨服奖励幂等结算、跨服称号发放与过期，并接入 Rank 荣誉中心。
- 四插件使用证据：
  - Superpowers：确认 Phase 15 成功标准、当前稳定点 `a08316f`、最小改动范围和 TDD 验证主线。
  - Browser Use：通过 in-app browser 打开 `http://127.0.0.1:5173/`，进入 Rank 荣誉中心，验证跨服模块、报名、结算反馈和 Console；仅有既有 Tailwind CDN warning。
  - Build Web Apps：检查新增跨服面板沿用主页/荣誉中心的深色商务手游基线、金色强调、`glass-panel`、`btn-gold`、紧凑资源栏和底部导航。
  - Game Studio：检查跨服入口和奖励反馈位于荣誉中心内页，不遮挡主页 HUD、底部导航和主流程，移动竖屏可读。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`。
- 浏览器/截图验收：Browser Use 截图确认跨服创业大赛、分池说明、跨服创业大赛榜、跨服商会榜、已报名状态和跨服奖励结算反馈；Console 当前只有既有 Tailwind CDN warning。
- 遗留风险：后台调整跨服分组的可视化管理属于 Phase 16 运营后台，本阶段已提供数据库配置结构和种子分组。
- 下一阶段入口：Phase 16。
- 是否可作为稳定点：可以。

### Phase 16：运营后台

目标：让运营可以不改代码管理游戏。

开发内容：

- [x] 后台登录。
- [x] 权限角色。
- [x] 玩家查询。
- [x] 公司财务查询。
- [x] 平台币余额和流水查询。
- [x] 平台币发放、扣减、修正。
- [x] 商品购买记录查询。
- [x] 外部支付订单预留查询。
- [x] VIP 查询和调整。
- [x] 称号配置、发放、回收。
- [x] 排行榜配置、奖励配置、手动结算、快照查询。
- [x] 成就配置、奖励配置、完成条件配置。
- [x] 知识条目配置、来源链接、采集日期、内容版本、审核状态。
- [x] 知识内容联网检索记录。
- [x] 邮件补偿。
- [x] 封禁/解封。
- [x] 商品、活动、任务、事件配置。
- [x] 区服管理。
- [x] 跨服分组。
- [x] 操作审计日志。

验收标准：

- 后台登录页独立存在，未登录不能进入后台任何管理页面。
- 后台整体结构符合稳重运营后台习惯，不以卡片模块堆积作为主要管理方式。
- 运营能查询玩家、公司、平台币、商品购买记录、VIP、称号、成就、知识解锁和排行榜。
- 运营能发放平台币和邮件补偿。
- 平台币和 VIP 后台操作必须校验权限。
- 高风险操作有二次确认和审计日志。
- 配置错误能被拦截。
- 服务器和数据库备份/回滚由宝塔面板处理，游戏后台不自建备份/回滚能力。
- 知识内容必须可审核、可标记来源、可记录版本。

阶段检查：

- 后台登录拦截测试。
- 后台菜单、筛选、表格、分页、详情和操作确认检查。
- 后台文案正式性检查。
- 后台权限测试。
- 平台币发放和扣减测试。
- VIP 调整测试。
- 称号发放和回收测试。
- 排行榜手动结算测试。
- 成就配置测试。
- 知识配置测试。
- 知识来源字段测试。
- 配置校验测试。
- 邮件补偿测试。
- 审计日志测试。
- `lint/typecheck/build` 全部通过。

阶段总结：

- 完成内容：新增运营后台数据闭环。后端提供 Admin 玩家查询、配置中心、审计日志、称号发放/回收、邮件补偿、封禁/解封、排行榜手动结算、跨服分组调整接口；新增玩家状态字段和后台邮件补偿记录；后台页面改为左侧菜单、顶部栏、筛选区、表格和操作区结构，覆盖玩家、平台币/VIP、称号/补偿、跨服分组、配置清单和审计日志。
- 四插件证据：Superpowers 用于确认 Phase 16 目标、TDD 和完成前验证；Browser Use 打开 `http://127.0.0.1:5174/` 验收 Admin 菜单、玩家查询、平台币/VIP、称号/补偿、配置清单、审计日志、跨服分组，并回到 `http://127.0.0.1:5173/` 确认前台壳层正常；Build Web Apps 检查 Admin 新页面延续稳重运营后台结构；Game Studio 检查后台操作不遮挡前台主流程，移动宽度下菜单、表单和操作区可读可滚动。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`。
- 浏览器/截图验收：Admin 已实测后台登录态恢复、玩家查询、配置清单、称号/补偿、平台币/VIP、跨服分组、审计日志；客户端 `http://127.0.0.1:5173/` 验收可正常打开。Console 仅有既有 Tailwind CDN warning。
- 遗留风险：当前权限角色按单一超管账号和所有高风险接口 Admin Token 校验闭环；后续如需要多人运营分岗，可在本阶段模型上扩展细粒度 RBAC。数据库备份/回滚继续按规划交由宝塔面板处理，游戏后台不自建备份/回滚。
- 下一阶段入口：Phase 17。
- 是否可作为稳定点：可以。

### Phase 17：数据埋点、监控与调参

目标：具备商业化调优能力。

开发内容：

- [x] 新手步骤埋点。
- [x] 任务完成率。
- [x] 成就完成率。
- [x] 知识点查看率。
- [x] 合规事件选择率。
- [x] 活动榜参与率。
- [x] 项目失败率。
- [x] 现金流崩盘点。
- [x] 负债率分布。
- [x] 融资成功率。
- [x] 员工离职率。
- [x] 商品点击率。
- [x] 平台币消耗、发放、存量统计。
- [x] VIP 经验和等级分布。
- [x] 商品购买转化率。
- [x] 留存统计。
- [x] 错误日志。
- [x] 接口耗时。
- [x] 平台币异常变动告警。
- [x] 外部支付异常预留告警。

验收标准：

- 能看到新手流失点。
- 能看到核心经营系统卡点。
- 能看到平台币发放、消耗和存量。
- 能看到 VIP 转化和商品购买漏斗。
- 能看到成就完成和知识内容消费情况。
- 能看到合规事件中不同选择的占比。
- 服务异常有日志和告警。
- 平台币异常变动可追踪到来源。

阶段检查：

- 埋点上报测试。
- 数据看板字段检查。
- 平台币统计口径检查。
- VIP 统计口径检查。
- 成就统计口径检查。
- 知识查看统计检查。
- 合规事件选择统计检查。
- 错误日志测试。
- 平台币异常告警测试。
- `lint/typecheck/build` 全部通过。

阶段总结：

- 完成内容：新增 Phase 17 商业化调优闭环。后端增加玩家埋点事件和 API 请求日志模型，提供 `POST /telemetry/events` 和 `GET /admin/analytics`；运营看板聚合玩家留存、任务/成就/知识查看、事件选择、项目失败、负债分布、融资成功、员工离职、平台币发放/消耗/存量、VIP 等级分布、商品点击和购买转化，并输出平台币异常变动与外部支付预留告警；管理端新增“数据看板”入口；前台在进入游戏、知识库加载和商城商品点击时上报最小埋点，不阻塞主流程。
- 四插件证据：Superpowers 用于 Phase 17 计划、TDD、验证前检查；Browser Use 打开 `http://127.0.0.1:5174/` 验收数据看板核心指标、漏斗、平台币、VIP 分布和告警，并在 `http://127.0.0.1:5173/` 验收排行榜知识库、商城商品点击和主 HUD 不被遮挡；Build Web Apps 检查 Admin 看板延续既有运营后台信息密度、表格和卡片风格；Game Studio 检查前台埋点只挂在现有入口和奖励/知识/商城流程上，不改变玩法节奏和移动端 HUD 结构。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 浏览器/截图验收：Admin 已实测数据看板显示玩家总数、留存、错误/慢接口、任务与玩法漏斗、平台币与商品转化、负债/VIP 分布和告警；客户端已实测恢复进入主页、打开 Rank 荣誉中心、查看知识库免责声明、打开商城并点击商品，埋点表中已出现 `tutorial_step`、`knowledge_view`、`shop_product_click`。Console 当前只有既有 Tailwind CDN warning；历史 `useCallback` 错误来自修复前热更新记录，当前页面已恢复正常。
- 遗留风险：活动榜参与率和现金流崩盘点当前按既有业务状态与活动榜未开启状态聚合展示，没有新增活动系统；外部支付仍为预留订单告警，不接真实支付回调。API 日志为应用内请求日志，后续上线前可再接入更完整的服务监控。
- 下一阶段入口：Phase 18。
- 是否可作为稳定点：可以。

### Phase 18：上线准备

目标：达到商业测试上线标准。

开发内容：

- [x] 生产环境配置。
- [x] 静态资源 CDN。
- [x] 宝塔面板站点、数据库、定时任务和服务管理配置。
- [x] Redis 持久化策略确认。
- [x] 灰度发布。
- [x] 压测。
- [x] 安全检查。
- [x] 防刷接口限制。
- [x] 平台币安全检查。
- [x] 外部支付预留接口安全检查。
- [x] 客服处理流程。
- [x] 开服流程文档。

验收标准：

- 支撑首批商业测试流量。
- 登录、平台币、VIP、存档、活动结算稳定。
- 可灰度发布。
- 服务器备份、数据库备份和回滚由宝塔面板负责，开发计划不自建备份/回滚系统。
- 客服能处理平台币、商品购买、VIP 和补偿问题。
- 外部支付接口预留不暴露未授权发货风险。

阶段检查：

- 压测报告。
- 平台币专项测试。
- VIP 专项测试。
- 宝塔面板部署配置检查。
- 灰度发布检查。
- 安全基础检查。
- 全量 `lint/typecheck/build/test` 通过。

阶段总结：

- 当前阶段：Phase 18 上线准备。
- 完成内容：新增 `docs/production-readiness.md`，收敛生产环境配置、静态资源 CDN、宝塔面板部署、Redis 持久化、灰度发布、压测口径、安全检查、客服处理和开服流程；后端新增 `GET /readiness` 依赖配置检查、Auth 基础防刷限流、外部支付预留商品金额校验，降低商业测试前的上线风险。
- 四插件使用证据：
  - Superpowers：确认当前稳定点 `5d525fc`、Phase 18 成功标准、TDD 主线和最小改动范围，只处理上线准备与安全护栏。
  - Browser Use：打开本地前台和 API，验收主页可进入、HUD 不被 Phase 18 后端护栏影响，并检查 `/readiness` 可返回依赖状态。
  - Build Web Apps：检查本阶段无新增前台页面，既有前台仍沿用深色商务手游基线；上线文档要求前台、后台构建产物和 CDN 缓存策略不改变现有 UI 风格。
  - Game Studio：检查本阶段未改玩法入口、奖励反馈和 HUD 结构；Auth 限流、readiness 和支付预留校验均在后端，不遮挡玩家主流程。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`、`git diff --check`。首次 `db:generate` 因本地 API dev 进程占用 Prisma Windows DLL 失败，停止 API 相关 dev/watch 进程后重跑通过；首次 `db:push` 因当前 shell 未加载 `DATABASE_URL` 失败，按本地 `.env` 注入进程环境后重跑通过。
- 浏览器/截图验收：Browser Use 打开 `http://127.0.0.1:3001/readiness`，确认返回 `ready` 且 MySQL、Redis 检查为 `pass`；打开 `http://127.0.0.1:5173/`，确认可恢复进入游戏主界面，顶部资源栏、左右悬浮入口、主线任务条和底部导航正常；点击“排行”可打开 Rank 荣誉中心。Console 当前仍有既有 Tailwind CDN warning，历史 `useCallback` 错误来自本阶段前热更新记录，当前页面已正常渲染。
- 遗留风险：本阶段完成仓库内上线准备和最小安全护栏，不执行真实服务器部署、真实 CDN 配置或真实压测；外部支付仍为预留订单，未接入真实支付回调、验签和自动发货。
- 下一阶段入口：Phase 19。
- 是否可作为稳定点：可以。

### Phase 19：赛季、限时活动、经营残局与长期运营

目标：让玩家跑通主线后仍有长期目标，形成 H5 网游持续留存和付费循环。

开发内容：

- [x] 赛季系统。
- [x] 赛季任务。
- [x] 赛季积分。
- [x] 赛季通行证。
- [x] 限时活动列表。
- [x] 活动报名。
- [x] 活动进度。
- [x] 活动结算。
- [x] 活动临时榜。
- [x] 活动商店。
- [x] 经营残局配置。
- [x] 经营残局进入、决策、评分、奖励。
- [x] 赛季称号、赛季成就、赛季知识卡。
- [x] 后台赛季、活动、奖励、残局配置。

验收标准：

- 主线完成后能进入长期目标界面。
- 活动未开启时不显示活动榜，开启后正常显示入口和剩余时间。
- 活动结束后不可继续提交进度，奖励、称号、成就记录保留。
- 活动奖励不会破坏现金、平台币、员工成长和 VIP 规则。
- 通行证购买消耗平台币并计入 VIP 经验。
- 经营残局初始状态固定，评分可重复验证。
- 后台可配置赛季、活动、奖励和经营残局。

阶段检查：

- 赛季状态流转测试。
- 活动开启/结束测试。
- 活动奖励幂等测试。
- 活动榜显示/隐藏测试。
- 通行证购买与 VIP 经验测试。
- 经营残局评分测试。
- 后台活动配置测试。
- `lint/typecheck/build` 全部通过。

阶段总结：

- 完成后端赛季、赛季任务、赛季积分、通行证购买、限时活动报名/进度/领奖、活动临时榜、活动商店、经营残局启动/评分/奖励闭环。
- 新增赛季称号 `AI风口先锋`、赛季成就 `AI 风口上榜` 和赛季知识卡 `AI Agent 风口活动复盘`；活动领奖完成赛季成就，成就领取解锁知识卡。
- 前台“活动/限时活动”入口进入赛季活动页，沿用主页深色商务手游基线，展示赛季任务、通行证、活动榜、活动商店和经营剧本；修复原生页面层级低于底部导航导致按钮被遮挡的问题。
- 后台配置中心新增赛季、活动、经营剧本配置可见性。
- 四插件检查：
  - Superpowers：确认 Phase 19 目标、成功标准、最小范围和当前稳定点 `070d37d`，按 TDD 先写 Phase 19 红测再实现。
  - Browser Use：本地验收 `http://127.0.0.1:5173/` 注册登录恢复、打开“活动”、推进赛季任务、活动报名/进度/领奖、活动商店兑换、经营剧本结算；验收 `http://127.0.0.1:5174/` 后台配置中心可见赛季/活动/剧本。
  - Build Web Apps：检查新增赛季页沿用当前主页 HTML/UI 基线，保持深色背景、金色强调、glass-panel、btn-gold、顶部资源栏和底部导航体系一致。
  - Game Studio：检查赛季页作为玩法入口不遮挡 HUD，奖励反馈、活动榜、商店、剧本按钮在移动端可读；发现并修复页面层级低于底部导航的点击遮挡问题。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`、`git diff --check`。首次 `db:generate` 因本地 API dev 进程占用 Prisma Windows DLL 失败，停止 API 相关 dev/watch 进程后重跑通过。
- 遗留风险：Phase 19 完成单赛季单活动单剧本的最小长期运营闭环，尚未做多赛季轮换、复杂活动日历和后台编辑表单。
- 下一阶段入口：Phase 20 已补充为首日、7日与长期付费深度闭环。
- 是否可作为稳定点：可以。

## 8. 每阶段四插件强制流程

每个 Phase 都必须经过 `Superpowers`、`Browser Use`、`Build Web Apps`、`Game Studio` 四插件检查，不再只限于视觉阶段。插件使用只服务当前阶段目标，不允许借机扩大范围、重构无关模块或新增未要求功能。

- 每个 Phase 开始前必须使用 `Superpowers` 做阶段目标、成功标准、最小改动范围和当前稳定点确认。
- 每个 Phase 涉及前端、页面、入口、HUD、弹窗、布局、底图叠层或可视交互时，必须使用 `Build Web Apps` 做视觉层级、布局一致性和移动端适配检查。
- 每个 Phase 涉及游戏主页、HUD、主场景、玩法入口、任务/奖励展示或沉浸感时，必须使用 `Game Studio` 做游戏界面体验检查。
- `Build Web Apps` 和 `Game Studio` 检查时，必须把当前主页 HTML 风格作为视觉基线，检查新增页面是否与主页在色彩、控件、入口密度、HUD 层级和移动端布局上保持一致。
- 每个 Phase 完成后必须使用 `Browser Use` 打开或刷新本地页面，做截图、交互和回归检查；纯后端阶段也要至少检查注册登录、进入主页和核心入口不被破坏。
- 如果某个插件运行失败，不能直接跳过；必须在阶段总结中记录失败原因、降级验证方式和未覆盖风险。
- 阶段提交前必须写明四插件使用证据：使用了什么、检查了什么、发现了什么、是否还有风险。

## 9. 每阶段统一代码检查规则

每个阶段完成后必须执行：

- 代码自检：确认所有改动都服务本阶段目标。
- 类型检查：前端、后端、后台 TypeScript 类型无错误。
- Lint：无明显规范错误。
- Build：前端、后端、后台均可构建。
- 单元测试：本阶段核心逻辑覆盖。
- 接口测试：本阶段 API 正常。
- 数据库检查：迁移可执行、字段命名清晰、索引合理。
- 幂等检查：奖励、平台币、VIP、排行榜、称号、结算类接口不可重复发放。
- 安全检查：客户端不可信，后台高危操作有权限和审计。
- 四插件检查：`Superpowers / Browser Use / Build Web Apps / Game Studio` 均已按本阶段目标使用，并在阶段总结中记录证据。
- UI 检查：移动端竖屏无遮挡、无文字溢出、关键按钮可点击。
- 底图叠层 UI 检查：凡使用底图加代码叠层的页面，必须遵守 `docs/ui-overlay-guidelines.md`，按底图外部框坐标、Browser Use 截图和单变量调整流程验收。
- 文案检查：禁止测试文案、占位文案、调试文案、临时英文文案、不符合真实创业或本地用户习惯的表达进入提交；按钮、空状态、错误提示、登录态、权限提示和后台操作提示必须是正式产品文案。
- 知识内容检查：联网来源必须优先官方或权威来源，且展示免责声明。
- 回归检查：注册登录、进入主界面、加载公司数据不被破坏。
- 阶段总结：必须按固定模板记录完成内容、四插件证据、测试结果、遗留风险、下一阶段入口和是否可作为稳定点。

## 10. 平台币安全规则

- 平台币余额只以后端数据库为准。
- 任何平台币增加、扣减、修正都必须写流水。
- 平台币流水必须记录来源：后台发放、后台扣减、商品购买、活动奖励、预留充值、系统补偿。
- 后台发放平台币必须有权限校验和审计日志。
- 高额度平台币发放必须二次确认。
- 商品购买必须先校验余额，再扣减，再发货，且整体在事务内完成。
- 商品购买接口必须防重复提交。
- 游戏内平台币消费必须计入 VIP 经验。
- 后台发放平台币不直接计入 VIP 经验，除非后续明确配置允许。
- 平台币不得由前端直接传最终余额。
- 外部支付接口预留时不能默认信任回调，后续接入时必须验签和幂等发货。

## 11. 阶段推进原则

- 每阶段必须完整闭环，不留半成品入口。
- 每阶段完成后再进入下一阶段。
- 高风险系统先做最小可用版本，再扩展。
- 平台币、VIP、奖励、财务结算必须以后端为准。
- 后续所有正式业务系统默认先落 MySQL/Prisma 数据模型、索引、幂等约束和正式种子数据，再开发 API 与前端交互。
- 账号、角色、经营资产、员工、任务、活动、平台币、VIP、排行榜、称号、成就、知识库、后台审计不得使用内存 Map 作为正式实现。
- 本地 MySQL 数据区作为上线前的真实联调环境，Navicat 只用于检查和核对数据，不作为游戏逻辑来源。
- 数据库账号、密码、密钥、支付回调密钥等敏感信息只通过本地环境变量或部署环境配置，不写入仓库文档、代码或种子文件。
- 不在未验证阶段继续叠加新系统。
- 每次阶段结束必须产出阶段总结：完成内容、测试结果、遗留风险、下一阶段入口。
- 所有系统设计由本计划托管，后续默认按本计划推进。
- 任务、活动、付费、员工、交互和经济内容必须优先参考配套设计文档，不允许实现阶段临时拼凑。
- 每个阶段提交前必须清理所有临时演示内容、测试账号展示文案、示例占位文案和与当前游戏设定不一致的文字；确需保留的示例数据必须命名为正式种子数据并进入配置或测试文件。
- 服务器备份、数据库备份和回滚使用宝塔面板能力，不在游戏项目内重复建设。

## Phase 20：首日、7日与长期付费深度闭环

目标：

- 将商业化从单一商城入口扩展为首日转化、7日留存转化、3-6 个月长期 LTV 深度。
- 补全主线、每日、支线任务，让公司档案、首单项目、首位员工、产品研发、财务复盘、资本选择、市场竞争、赛季活动形成可跑闭环。
- 新增道具与背包资产，让任务、商城、特权、通行证、活动商店能发放真实道具。
- 扩充员工池到 24 名，作为员工收集、岗位补位、猎头招募和长期养成的付费核心。

完成内容：

- 新增道具配置、玩家背包、道具流水；首批道具包括培养手册、猎头券、定向猎头函、员工好感礼物、风险保险、行动力饮料、项目加速券、市场情报、财务顾问卡、赛季经验券、限定称号碎片和办公室皮肤券。
- 任务配置扩展为 8 个主线、7 个每日/长期日常、7 个支线方向；日常奖励改为现金、行动力、道具、声望为主，不再大量发平台币。
- 商城新增首充、每日礼包、经营周卡、7日成长基金、员工成长礼包、定向猎头礼包、市场冲刺和项目交付礼包；商业、特权、通行证继续保持职责分流。
- 通行证扩展为多赛季任务线，任务和活动商店都能发真实道具；购买通行证额外发放赛季经验券、限定称号碎片和办公室皮肤券。
- 前台背包页改为真实资产中心；商城、特权、通行证、任务页展示道具奖励；员工页展示普通招募、猎头招募、定向猎头三层付费方向。

阶段总结：

- 四插件使用证据：
  - Superpowers：按执行计划拆解任务、道具、员工、特权/通行证和验证步骤。
  - Browser Use：使用本地浏览器验收主页、商城、特权、通行证、背包和员工页。
  - Build Web Apps：检查新增资产中心和付费页仍沿用深色商务手游基线。
  - Game Studio：检查任务条、右侧入口、底部导航和付费页不遮挡主流程。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：当前已完成真实道具发放和展示，但公司等级仍只是展示字段，尚未接入快节奏经验、倍率、随机经营任务和满级后经验去向。
- 下一阶段入口：Phase 21，全功能开放、快节奏公司等级、主线扩容、随机经营任务和经验倍率商业化。
- 是否可作为稳定点：命令和浏览器验收通过后可以作为新稳定点。

## Phase 21：全功能开放、快节奏等级与随机经营任务

目标：

- 所有核心功能默认开放，不再用公司等级硬锁员工、业务、市场、财务、融资、贷款、商会、排行、商业、特权、通行证和背包入口。
- 公司等级改为玩家对比、首日冲榜、基金节点、主线推进、经验倍率和阶段奖励，等级上限固定为 80。
- 付费玩家通过经营周卡、成长基金和通行证经验倍率，目标 1 天满级；免费活跃玩家目标 2-3 天满级。
- 将主线从当前 8 个骨干任务扩展为 60-80 个短任务，覆盖公司启动、员工、项目、产品、财务、资本、市场、商会、赛季和跨服。
- 基于根目录原型 HTML 中的随机经营事件弹窗，建设正式随机经营任务系统，覆盖项目、员工、产品、财务、市场、商会、赛季和风险事件。
- 补全行动力、声望和 VIP15 体系：行动力代表创始人精力，声望代表品牌口碑，VIP 代表身份、便利和轻量经营效率。
- VIP 上限扩展到 VIP15，新号创建公司后永久赠送 VIP3；赠送写入 `vipExperience = 3000`，但 `totalSpent = 0`，不计入真实消费和付费转化。

设计规则：

- 等级不卖功能，不阻止玩家进入核心玩法；主线负责解释功能价值，等级负责奖励、对比和付费效率承接。
- 经验来源包括主线任务、每日任务、随机经营任务、项目/产品/员工/市场操作和赛季任务。
- 经验倍率采用中强方案：免费 1.0 倍，经营周卡 1.5 倍，周卡加成长基金 1.8 倍，周卡加成长基金加通行证最高 2.0 倍封顶。
- 倍率只影响公司经验，不放大现金、平台币和排行榜结算奖励，避免经济膨胀。
- 成长基金改为首日冲级基金加满级后深度基金，节点建议为 Lv.5、10、15、20、30、40、50、60、70、80。
- 满级后经验转为创业声望积分、赛季贡献或满级宝箱进度，避免满级玩家经验浪费。
- 行动力不限制进入功能页面，只限制高收益经营动作；基础上限 120，每 10 分钟恢复 10 点，离线恢复到上限。
- 行动力饮料普通购买每日限 3 次，每日经营礼包每日限 1 次；VIP 可提高上限和限购次数，但不得形成无限购买。
- 声望不作为普通消耗货币滥用，主要影响融资成功率、估值议价、市场竞争、商会贡献、排行榜展示和随机任务选项。
- VIP 权益不得直接清空负债、免除经营失败、直接提高排行榜名次或提供不可替代玩法。

随机经营任务规则：

- 在线每 3-8 分钟有概率触发一次；首次进入游戏前 2 分钟不弹，商城购买、领取奖励和通行证操作中不弹。
- 普通玩家每日最多 6 次，周卡/月卡玩家最多 9 次，通行证玩家额外获得赛季随机任务。
- 随机任务不是广告弹窗，必须是经营选择：标题 1 行、说明 2 行以内、2-3 个决策、明确消耗、风险和奖励。
- 随机任务可以消耗或使用行动力饮料、风险保险、市场情报、财务顾问卡、定向猎头函等道具改变结果。
- 可关闭的随机任务进入专属经理待办流，不强迫立即处理；高价值任务可显示倒计时，普通任务不制造焦虑。

商业承接：

- 经营周卡展示 1.5 倍公司经验、每日行动力、培养材料和风险保险。
- 成长基金展示等级节点奖励和经验倍率参与上限。
- 通行证展示赛季任务、额外随机赛季任务和经验倍率参与上限。
- 商城补充随机经营任务相关道具包，例如风险保险包、顾问卡包、员工培养包、市场情报包和行动力包。
- 付费推荐只在任务结果、道具不足或基金节点接近时出现，不在任务开始前硬弹购买。
- VIP3 作为开服基础权益强化首日福利感；VIP4-VIP15 承接周卡、成长基金、通行证、员工养成、商会、跨服、外观和长期身份付费。

验收标准：

- 新号进入后所有核心功能入口可打开。
- 完成主线和随机经营任务后，公司经验和等级快速增长。
- 周卡、成长基金、通行证经验倍率显示和计算正确，最高不超过 2 倍。
- 付费玩家在高活跃 1 天内可满级，免费活跃玩家 2-3 天可满级。
- 随机经营任务不会遮挡主页 HUD，不频繁打断首日任务。
- 满级后经验仍有明确去向。
- 新号 VIP 中心显示 VIP3，且 `vipExperience = 3000`、`totalSpent = 0`。
- VIP0-VIP15 等级阈值递增，平台币消费和通行证购买能从 VIP3 继续累加 VIP 经验。
- 行动力恢复、消耗、限购和溢出规则以后端服务器时间为准，不能依赖前端计时。

### 阶段总结必须包含

- 当前阶段：Phase 21 第一实施批次：全功能开放、快节奏等级与随机经营任务。
- 完成内容：新增公司经验字段、80 级成长计算、任务奖励公司经验、周卡/月卡/成长基金经验倍率基础；新增行动力服务器时间恢复字段和随机任务行动力消耗；新增随机经营任务配置、玩家随机任务、生成、处理和稍后处理 API；新号创建时永久写入 VIP3 起步 `vipExperience = 3000` 且 `totalSpent = 0`；种子配置扩展到 VIP15；前台主页等级进度、专属经理待办中心、随机任务独立短决策弹窗和行动力/HUD 刷新已接入。
- 四插件使用证据：
  - Superpowers：以当前稳定点 `e83fe79` 为起点，按 Phase 21 目标收敛最小实现范围，保留 60-80 主线扩容为后续批次，先闭环经验、行动力、VIP15 和随机任务。
  - Browser Use：刷新 `http://127.0.0.1:5173/`，登录进入主页，确认右侧 6 入口存在；点击专属经理进入经营提醒/随机任务待办中心；随机任务从待办中心打开独立居中短决策弹窗，选择后刷新现金、行动力、声望和公司经验。
  - Build Web Apps：检查新增前台只复用现有 `page-container`、`glass-panel`、`btn-gold`、资源条和深色商务手游风格；专属经理保持全覆盖待办中心，随机任务弹窗沿用根目录原型 `event-overlay` 的深色遮罩和短决策风格。
  - Game Studio：检查随机任务是短决策弹窗，显示行动力、风险、奖励和稍后处理；弹窗不连续叠加，关闭后任务保留为 pending 并进入专属经理待办，不遮挡支付、登录、选服、创建公司和主系统页。
- 验证命令：`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run test -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`git diff --check`。首次 `db:push` 因当前 shell 未加载 `DATABASE_URL` 失败，按根目录 `.env` 注入进程环境后重跑成功；`db:push` 内部 generate 阶段出现 Windows Prisma DLL 占用提示，但此前单独 `db:generate` 已成功，数据库同步和 seed 已成功。
- 浏览器/截图验收：Browser Use 需确认专属经理随机任务待办可显示、弹窗可打开/关闭/处理并刷新 HUD；页面仍保持深色商务手游 UI，右侧入口和底部导航未被遮挡。
- 遗留风险：60-80 个短主线任务尚未全量扩容；行动力限购目前写入设计和商品购买上限基础，尚未做独立每日购买计数；通行证额外赛季随机任务、满级宝箱和经验去向 UI 仍需下一批实现；既有老号不会被强制迁移到 VIP3，VIP3 赠送只在新建角色和新建钱包时生效。
- 下一阶段入口：Phase 21 第二实施批次：主线 60-80 短任务扩容、行动力饮料使用/限购、满级后经验去向、通行证赛季随机任务和基金倍率展示。
- 是否可作为稳定点：本批次通过命令和 Browser Use 验收后可作为阶段稳定点。

### Phase 21 第二实施批次阶段总结：主线扩容与渐进显示

- 当前稳定点：`de03c9e feat: split random tasks into modal flow`。
- 完成内容：普通任务配置从 22 条扩展到 78 条，其中主线从 8 条扩展到 64 条，每日任务保持 7 条，支线任务保持 7 条；新增 56 条短主线覆盖公司启动、员工、项目、产品、财务、融资贷款、市场、商会、排行、跨服、赛季、通行证、VIP、特权、行动力、声望和满级去向。
- 主线显示规则：主线不再一次性铺满任务页；后端按主线顺序只返回已领取主线和当前第一条未领取主线，领取后前台刷新任务列表并展示下一条，避免玩家被 60+ 主线同时淹没。
- 主线推进规则：所有核心功能仍默认开放；主线点击“前往”会推进当前短目标并跳转到对应系统，用任务解释玩法价值，不用等级硬锁功能。
- 四插件使用证据：
  - Superpowers：按 Phase 21 第二实施批次收敛目标，优先补主线扩容和渐进出现，暂不混入行动力限购、满级宝箱和通行证额外随机任务。
  - Browser Use：本地浏览器需验收任务页只显示当前主线、领取后刷新下一条，主页主线任务条继续可读且不遮挡 HUD。
  - Build Web Apps：前台不新增任务页视觉体系，复用现有任务页、主线条、`btn-gold` 和深色商务手游基线。
  - Game Studio：检查任务渐进出现不会打断主页主流程，点击主线“前往”只做轻引导，不用弹窗堆叠和等级硬锁。
- 验证命令：`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run test -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`npm run db:seed -w @wenziyouxi/api`、`git diff --check`。首次 `db:seed` 因当前 shell 未加载 `DATABASE_URL` 失败，按根目录 `.env` 注入进程环境后重跑成功。
- 遗留风险：行动力饮料使用/每日限购、满级后经验去向 UI、通行证额外赛季随机任务仍在下一批；本批先补足主线体量和任务渐进体验。
- 下一阶段入口：Phase 21 第三实施批次：行动力饮料使用与限购、满级后经验去向、通行证赛季随机任务和基金倍率展示。

### Phase 21 第三实施批次阶段总结：主线真实动作绑定第一批

- 当前稳定点：`b44bbd9 fix: bind remaining task completion actions`。
- 完成内容：将 12 条已扩容主线接入真实经营动作，包括员工招募、员工培养、股权激励、项目启动、项目推进、项目结算、产品启动、产品重构、市场进入、竞争对手动作、商会互助和赛季任务推进。
- 任务完成规则：本批不改前台任务展示、不新增后端模型，只让主线任务从“有配置、有引导”推进到“玩家做对应经营动作即可完成”；核心功能仍全部开放，不通过等级硬锁。
- 四插件使用证据：
  - Superpowers：按当前阶段目标收敛为第一批真实动作绑定，先修正任务可完成性，不混入行动力限购、满级宝箱和通行证额外随机任务。
  - Browser Use：本批无前台 UI 改动，浏览器验收重点保留在主页入口、任务页和经营系统可访问；任务完成性由 API 集成测试覆盖。
  - Build Web Apps：确认本批不改变页面组件、HUD、全覆盖内页和深色商务手游视觉基线。
  - Game Studio：确认任务推进来自经营动作本身，不增加打断式弹窗，不影响主页 HUD、右侧入口、底部导航和核心流程。
- 验证命令：`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`git diff --check`。
- 遗留风险：财务、融资、贷款、排行榜、跨服、VIP、特权、通行证、成长基金、行动力与声望后续主线仍需继续绑定真实动作；行动力恢复、饮料使用、每日限购和满级后经验去向仍在下一批。
- 下一阶段入口：Phase 21 第四实施批次：剩余主线真实动作绑定第二批，然后进入行动力恢复、饮料使用、限购、满级经验去向和通行证额外赛季随机任务。

### Phase 21 第四实施批次阶段总结：主线真实动作绑定第二批

- 当前稳定点：`d31a896 feat: bind expanded main tasks to gameplay actions`。
- 完成内容：将 11 条财务、资本和对比目标主线接入真实经营动作，包括制定首周预算、阅读经营日报、制定现金流预算、优化成本结构、准备贷款方案、整理投资人名单、准备路演材料、复核融资条款、设定本服排行目标、了解跨服目标和规划声望成长。
- 任务完成规则：本批只在现有成功接口上补任务推进，不新增 UI、数据库字段或新接口；玩家查看财务、结算经营日、申请贷款、查看融资、启动并结算融资、查看排行榜和跨服中心即可推动对应主线。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认财务/资本/排行主线缺少真实动作推进，再补最小 `advanceTask` hook。
  - Browser Use：本批无前台 UI 改动，浏览器验收保留为本地页面可打开和 API 健康；真实完成性由 API 集成测试覆盖。
  - Build Web Apps：确认本批不改 React 页面、不增加 HUD 元素、不改变深色商务手游 UI 基线。
  - Game Studio：确认任务推进来自经营动作，不新增弹窗或强引导，不遮挡主页、右侧入口、任务条和底部导航。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "advances finance capital and ranking main tasks" --import tsx apps/api/test/http.test.ts`、`npm run typecheck -w @wenziyouxi/api`。
- 遗留风险：VIP、特权、通行证、活动商店、行动力计划和满级去向主线仍需下一小批单独绑定；活动商店链路在本批测试拆分时出现超时，需要下一批先做判别性排查再实现，不在本批叠加试探性补丁。
- 下一阶段入口：Phase 21 第五实施批次：VIP/特权/通行证/活动商店/背包主线真实动作绑定，然后进入行动力恢复、行动力饮料使用、每日限购和满级经验去向。

### Phase 21 第五实施批次阶段总结：VIP 特权通行证与背包主线绑定

- 当前稳定点：`3ed8a9d feat: bind finance ranking main tasks`。
- 完成内容：将 8 条剩余商业化与长期目标主线接入真实动作，包括查看通行证价值、规划活动商店、查看 VIP 起步权益、评估经营周卡、查看成长基金、查看基金节点、制定行动力计划和了解满级去向。
- 任务完成规则：本批只在现有成功接口上补任务推进，不新增数据库字段、不新增前台 UI、不改变购买模型；玩家查看 VIP、查看背包、购买经营周卡、购买成长基金、查看通行证、购买通行证和购买活动商店道具即可推动对应主线。
- 四插件使用证据：
  - Superpowers：按 TDD 和完成前验证流程，先用失败断言确认 VIP/特权/通行证/活动商店/背包主线缺少真实动作，再补最小 `advanceTask` hook。
  - Browser Use：本批无前台 UI 改动，浏览器验收保留为本地页面可打开、主页 HUD 不变和 API 健康可用；主线完成性由 API 集成测试覆盖。
  - Build Web Apps：确认本批不改 React 页面、不增加右侧入口、不改变特权/通行证/商业的深色商务手游视觉基线。
  - Game Studio：确认任务推进来自玩家主动查看或购买动作，不新增打断式弹窗，不遮挡主页 HUD、右侧 6 入口、主线任务条和底部导航。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "lists wallet and buys shop products" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "phase 19 runs season activity pass rewards" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`git diff --check`。
- 遗留风险：行动力恢复机制、行动力饮料使用、每日限购、满级后经验去向和通行证额外赛季随机任务仍未实现；下一批开始前必须先确认行动力作为创始人精力的恢复、消耗、返还和商业边界。
- 下一阶段入口：Phase 21 第六实施批次：行动力恢复、行动力饮料使用、每日限购、随机任务行动力增减、满级经验去向和通行证额外赛季随机任务。

### Phase 21 第六实施批次阶段总结：行动力饮料与每日礼包限购

- 当前稳定点：`244b29d feat: bind privilege pass main tasks`。
- 完成内容：补齐行动力饮料的真实使用闭环，新增背包道具使用接口，行动力饮料使用后扣减 1 个道具并增加 40 行动力；每日经营礼包改为按服务器日期限购，同一天只能购买 1 次，次日可重新购买。
- 规则确认：既有行动力服务器时间恢复、随机任务行动力消耗/不足拦截和满级后经验转声望的后端规则已存在，本批不重复改造；本批只补“饮料可用”和“每日礼包不是终身限购”两个缺口。
- 前台变化：背包页继续沿用全覆盖深色玻璃风格，优先选中行动力饮料；有行动力饮料时按钮显示“使用”，成功后刷新行动力 HUD 和背包数量；非可用道具仍显示“查看用途”且不开放误用。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认 `/inventory/use` 缺失和每日礼包限购无法跨日重置，再补最小实现。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后确认主页可进入背包页；当前账号背包只有风险保险时按钮保持禁用，不误开放行动力饮料使用。
  - Build Web Apps：前台只在现有背包页按钮上接入使用状态，不新增页面、不改变右侧入口、不改变深色商务手游基线。
  - Game Studio：行动力补给从背包主动操作进入，不用弹窗打断主流程；HUD 行动力刷新来自玩家明确使用道具，不遮挡主页、任务条和底部导航。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "uses action drink" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：通行证额外赛季随机任务、满级宝箱 UI、更多经营道具的具体使用效果仍未实现；下一批不应继续扩行动力基础规则，优先做通行证额外随机任务和满级后奖励展示。
- 下一阶段入口：Phase 21 第七实施批次：通行证额外赛季随机任务、满级宝箱/满级经验去向前台展示、风险保险/市场情报/财务顾问卡等经营道具使用规则。

### Phase 21 第七实施批次阶段总结：通行证额外赛季随机任务与满级去向展示

- 当前稳定点：`1e673a6 feat: add action drink usage`。
- 完成内容：通行证开通后，随机任务每日上限增加 1，并额外开放 1 个可见随机任务槽；额外槽优先生成 `season` 类赛季随机任务，普通玩家仍保持每日 6 次和 3 个可见待办。
- 前台变化：通行证页说明“开通后每日额外获得 1 个赛季随机任务”；公司已满级时，主页等级区域和通行证页展示满级后经验进入声望转换，并显示当前溢出经验。
- 规则边界：本批没有改行动力恢复、饮料使用、每日礼包限购和随机任务结算收益；风险保险、市场情报、财务顾问卡等经营道具使用规则仍留到后续单独批次。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认通行证购买后随机任务中心仍返回 `dailyLimit = 6`，再补最小实现。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认通行证页新增“开通后每日额外获得 1 个赛季随机任务”说明；购买通行证后按钮变为“通行证已开通”；进入专属经理随机任务页后显示今日处理 `0/7`，并出现“赛季运营 / 赛季风口机会”待办。
  - Build Web Apps：前台只复用现有深色玻璃面板、资源条和通行证页结构，不新增视觉体系。
  - Game Studio：额外赛季随机任务仍从专属经理待办进入，不新增强制弹窗，不遮挡主页、背包、通行证和任务主流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "season pass adds one daily season random task" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：满级宝箱仍只是设计方向，尚未实现宝箱进度和领奖；风险保险、市场情报、财务顾问卡等经营道具尚未接入随机任务结果修正。
- 下一阶段入口：Phase 21 第八实施批次：单独实现经营道具使用规则，优先从风险保险或市场情报中选择一条最小闭环。

### Phase 21 第八实施批次阶段总结：风险保险随机任务修正

- 当前稳定点：`31032b8 feat: add pass season random task`。
- 完成内容：风险保险接入随机经营任务结算；玩家在适用的非赛季随机任务中可选择使用 `risk-insurance`，结算成功后扣减 1 个风险保险并写入道具流水，现金或声望的负向结果按减半后的损失结算。
- 前台变化：随机任务弹窗在适用任务且背包有风险保险时显示“风险保险 xN”开关；勾选后可用决策按钮展示“使用风险保险”，结算成功后刷新 HUD、随机任务状态和背包库存。
- 规则边界：风险保险只降低损失，不完全取消失败；不减免行动力消耗，不放大公司经验，不影响赛季额外随机任务和排行榜结算；市场情报、财务顾问卡仍留到后续批次单独实现。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认传入 `modifierItemId = "risk-insurance"` 时随机任务仍按原始损失结算，再补最小实现。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认专属经理随机任务弹窗出现“风险保险 x2”开关；勾选后决策按钮显示“使用风险保险”；结算后提示“风险保险已生效，降低了本次经营损失”；背包页仍可看到剩余风险保险。
  - Build Web Apps：前台只在现有随机任务弹窗内新增一条紧凑经营道具栏，沿用深色遮罩、玻璃面板和金色/绿色强调，不新增页面和入口。
  - Game Studio：风险保险属于玩家主动选择的经营缓冲，不强制弹购买，不连续叠加弹窗；弹窗仍保留标题、来源、说明、2 个决策和稍后处理，未遮挡主流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "uses risk insurance" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`。
- 遗留风险：市场情报、财务顾问卡尚未接入随机任务结果修正；满级宝箱仍只是展示方向，尚未实现进度和领奖。
- 下一阶段入口：Phase 21 第九实施批次：市场情报接入市场/赛季随机任务，继续保持每次随机任务最多使用 1 个经营道具。

### Phase 21 第九实施批次阶段总结：市场情报随机任务修正

- 当前稳定点：`815b268 feat: apply risk insurance to random tasks`。
- 完成内容：市场情报接入市场/赛季随机经营任务结算；玩家在适用任务中可选择使用 `market-intel`，结算成功后扣减 1 个市场情报并写入道具流水；正向声望提升 20%，负向声望损失降低 20%，正向公司经验提升 10%。
- 前台变化：随机任务弹窗的经营道具栏从风险保险单一路径扩展为按任务类型选择修正道具；市场/赛季任务显示“市场情报 xN”和“优化本次市场判断”，勾选后决策按钮展示“使用市场情报”，结算后刷新 HUD、随机任务状态和背包库存。
- 规则边界：市场情报不影响现金、行动力、平台币、排行榜名次和每日随机任务上限；同一随机任务仍最多使用 1 个经营道具，不与风险保险叠加；财务顾问卡仍留到后续批次单独实现。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认 `modifierItemId = "market-intel"` 尚不能修正市场随机任务结果，再补最小实现并复跑风险保险回归。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认专属经理中“竞品突然降价”弹窗显示“市场情报 x3”；勾选后决策按钮显示“使用市场情报”；结算后提示“市场情报已生效，优化了本次市场判断”，行动力 HUD 从 `200/150` 更新到 `175/120`，背包市场情报从 3 扣到 2。
  - Build Web Apps：前台继续复用第八批次的随机任务道具栏，不新增入口、不改变深色商务手游基线，只替换适用任务中的文案、开关状态和按钮附加说明。
  - Game Studio：市场情报作为玩家主动勾选的单次经营判断修正，不强制弹购买，不遮挡专属经理待办列表；随机任务弹窗仍保留标题、来源、说明、2 个决策和稍后处理。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "uses market intel" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "uses market intel|uses risk insurance" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：财务顾问卡尚未接入财务/融资/贷款类随机任务；满级宝箱仍只是展示方向，尚未实现进度和领奖；本地浏览器回归中曾触发既有成就同步并发唯一键崩溃，重启 API 后本批市场情报链路可完成。
- 下一阶段入口：Phase 21 第十实施批次：财务顾问卡接入财务/融资/贷款随机任务，继续保持每次随机任务最多使用 1 个经营道具。

### Phase 21 第十实施批次阶段总结：财务顾问卡随机任务修正

- 当前稳定点：`d5fd79f feat: apply market intel to random tasks`。
- 完成内容：财务顾问卡接入财务/融资/贷款类随机经营任务结算；玩家在适用任务中可选择使用 `finance-advisor-card`，结算成功后扣减 1 张财务顾问卡并写入道具流水；正向现金提升 20%，负向现金损失降低 20%，正向声望提升 10%，负向声望损失降低 20%，正向公司经验提升 10%。
- 前台变化：随机任务弹窗的经营道具栏继续复用单一道具开关；财务/融资/贷款任务优先显示“财务顾问卡 xN”和“优化本次现金流判断”，勾选后决策按钮展示“使用财务顾问卡”，结算后刷新 HUD、随机任务状态和背包库存。
- 规则边界：财务顾问卡不影响行动力、平台币、排行榜名次和每日随机任务上限；同一随机任务仍最多使用 1 个经营道具，不与风险保险或市场情报叠加；当前配置中已存在财务类随机任务，融资/贷款类适用范围先在规则层开放，后续新增对应任务配置时自动生效。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认 `modifierItemId = "finance-advisor-card"` 会返回 `ITEM_NOT_USABLE`，再补最小实现并复跑三类经营道具回归。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认专属经理中“现金流预警复核”弹窗显示“财务顾问卡 x2”；勾选后决策按钮显示“使用财务顾问卡”；结算后提示“财务顾问卡已生效，优化了本次现金流判断”，行动力 HUD 从 `240/120` 更新到 `225/120`，背包财务顾问卡从 2 扣到 1。
  - Build Web Apps：前台只扩展既有随机任务道具栏的数据来源和文案，不新增页面、不改变深色商务手游基线，不引入新的视觉组件。
  - Game Studio：财务顾问卡仍由玩家在短决策弹窗内主动勾选，不强制弹购买；专属经理待办列表、详情和决策弹窗没有新增遮挡层或连续弹窗。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "uses finance advisor" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "uses finance advisor|uses market intel|uses risk insurance" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：满级宝箱仍只是展示方向，尚未实现进度和领奖；融资/贷款类随机任务配置尚未扩容，本批只完成财务顾问卡对对应类别的通用规则接入。
- 下一阶段入口：Phase 21 第十一实施批次：满级宝箱/满级经验去向进度和领奖闭环，或先补融资/贷款类随机任务配置扩容，二者需择一单独实施。

### Phase 21 第十一实施批次阶段总结：满级宝箱进度与领奖闭环

- 当前稳定点：`d599bea feat: apply finance advisor to random tasks`。
- 完成内容：公司成长接口返回满级宝箱进度；新增 `POST /company/growth/full-level-chest/claim` 领奖接口；满级后每 500 点溢出经验生成 1 个宝箱，可领取数量按已获得和已领取差值计算。
- 奖励规则：每个满级宝箱奖励声望 300、行动力 40、赛季经验券 1；赛季经验券写入背包，流水来源为 `full_level_chest`；重复领取会返回 `FULL_LEVEL_CHEST_NOT_READY`。
- 前台变化：主页等级区域在满级后显示“满级宝箱 进度/500”和可领取数量；通行证页满级区展示当前溢出经验、已领/已获得、奖励内容和领奖按钮；领取成功后刷新 profile、背包和成长信息，按钮变为“积累中”。
- 规则边界：不新增数据库表和迁移；不改变既有满级经验转声望规则；不发放平台币；已领取数量复用现有道具流水统计，避免为单一奖励新增状态表。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认满级宝箱领奖接口缺失，再补后端、前台和回归验证。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认页眉显示“满级宝箱 0/500 · 可领 1”；通行证页显示“领取宝箱”；点击后提示“满级宝箱奖励已领取。”，按钮变为“积累中”；背包出现“赛季经验券 1”。
  - Build Web Apps：前台只扩展既有主页等级区域和通行证满级区，不新增页面、不改变深色商务手游基线。
  - Game Studio：满级宝箱从通行证页主动领取，不强制弹窗、不遮挡主页 HUD、右侧入口、背包和主流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "claims full level chest" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：融资/贷款类随机任务配置尚未扩容；浏览器回归中再次触发既有成就同步并发唯一键崩溃，重启 API 后满级宝箱链路可完成，本批不混入该无关修复。
- 下一阶段入口：Phase 21 第十二实施批次：融资/贷款类随机任务配置扩容，补足财务顾问卡适用内容池。

### Phase 21 第十二实施批次阶段总结：融资贷款随机任务扩容

- 当前稳定点：`bf66094 feat: add full level chest rewards`。
- 完成内容：随机经营任务池新增融资类 `random-funding-term-sheet` 和贷款类 `random-loan-rate-review`，补足财务顾问卡对 `funding`、`loan` 类任务的真实内容承载。
- 规则边界：本批只新增任务配置和测试覆盖，不改随机任务生成算法、不改每日上限、不改财务顾问卡结算公式、不新增前台组件；既有市场情报、风险保险和财务类任务行为保持不变。
- 前台表现：专属经理随机任务列表可出现“投资条款临时调整”和“银行贷款利率复核”；打开融资任务弹窗后沿用既有经营道具栏，显示“财务顾问卡 xN”和“优化本次现金流判断”，勾选后决策按钮追加“使用财务顾问卡”。
- 四插件使用证据：
  - Superpowers：按 TDD 先写失败测试，确认任务池缺少 `funding`、`loan` 随机任务，再补最小配置并复跑全量 API 测试。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用本地 QA 账号验收，确认专属经理出现融资任务“投资条款临时调整”；弹窗显示“财务顾问卡 x2”；勾选并结算后提示“财务顾问卡已生效，优化了本次现金流判断”，HUD 从 `360/120` 刷新到 `345/120`，背包财务顾问卡从 2 扣到 1，并生成贷款任务“银行贷款利率复核”待办。
  - Build Web Apps：前台无新增视觉体系，只复用既有专属经理列表、短决策弹窗、道具开关和深色商务手游基线。
  - Game Studio：新增融资/贷款随机任务仍从专属经理待办进入，不强制弹出购买或多层弹窗，不遮挡主页 HUD、底部导航和背包主流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "lists funding and loan random tasks" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`npm run db:seed -w @wenziyouxi/api`。
- 遗留风险：真实随机任务生成仍会把赛季任务放入普通账号任务池，本批未改生成算法；既有成就同步并发唯一键风险仍未处理。
- 下一阶段入口：Phase 21 第十三实施批次：修正随机任务生成池边界，普通账号不应生成赛季随机任务；同时处理成就同步并发唯一键崩溃。

### Phase 21 第十三实施批次阶段总结：随机任务池边界与成就同步止崩

- 当前稳定点：`b9e4094 feat: add capital random tasks`。
- 完成内容：普通账号随机任务通用生成池排除 `season` 配置，避免没有通行证时生成“赛季风口机会”；通行证账号仍保留额外赛季随机任务入口。
- 止崩处理：成就进度同步遇到并发唯一键竞争时，改为捕获唯一键冲突后重新读取当前进度并更新，避免 `PlayerAchievement_profileId_achievementId_key` 竞争导致 API 崩溃。
- 规则边界：本批不改每日处理上限、可见随机任务数量、通行证额外任务数、随机任务奖励公式和经营道具修正效果；不新增前台组件。
- 前台表现：普通账号进入专属经理后只展示普通随机任务；成就、称号、知识等初始加载不应再因并发成就同步触发 API 崩溃。
- 四插件使用证据：
  - Superpowers：按 TDD 先写随机任务查询边界和成就并发唯一键恢复测试，再补最小实现并复跑相关回归。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用新建普通 QA 账号验收，确认专属经理随机任务列表没有“赛季风口机会”；API 健康检查仍返回 200，错误日志为空。
  - Build Web Apps：本批无前台组件改动，不改变深色商务手游视觉基线和专属经理信息层级。
  - Game Studio：随机任务仍从专属经理主动进入，不新增弹窗、不遮挡主页 HUD、随机任务详情和底部导航。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "random task config query|achievement sync recovers" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "regular random task generation|season pass adds one daily season random task|random task config query|achievement sync recovers" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：随机任务生成仍是按配置顺序补齐，可继续单独评估公平轮换或任务权重；本批只修普通账号赛季任务越界和成就同步崩溃。
- 下一阶段入口：Phase 21 第十四实施批次：可继续做随机任务生成公平轮换/权重规则，或进入下一条明确系统主线。

### Phase 21 第十四实施批次阶段总结：随机任务公平轮换收口

- 当前稳定点：`9e57c89 fix: guard random tasks and achievement sync`。
- 完成内容：随机任务普通补齐逻辑从单纯按配置顺序取前几项，调整为优先选择当天尚未出现过的类别；未出现类别不足时，再按既有 `sortOrder + createdAt` 稳定顺序兜底。
- 规则边界：普通账号继续排除 `season`；通行证账号仍优先额外生成 1 个赛季随机任务；每日处理上限、可见任务数量、任务过期时间、行动力、奖励和三类经营道具修正公式均不改变。
- 前台表现：专属经理仍沿用既有随机任务列表、详情和短决策弹窗；首屏随机任务类别更分散，不新增权重展示、后台配置页或前台入口。
- 四插件使用证据：
  - Superpowers：按 TDD 先写公平选择失败测试，确认缺少类别优先选择函数，再补最小实现并接入生成链路。
  - Browser Use：刷新 `http://127.0.0.1:5173/` 后使用普通 QA 账号验收，确认专属经理首屏随机任务没有“赛季风口机会”，且展示项目、员工、品牌等不同类别任务。
  - Build Web Apps：本批无前台组件改动，不新增页面、不改变深色商务手游视觉基线。
  - Game Studio：随机任务仍从专属经理主动进入，列表、详情和短决策弹窗不遮挡主页 HUD 与底部导航。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "fair random task selection" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "random task" --import tsx apps/api/test/http.test.ts`、`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 遗留风险：本批只做轻量公平轮换，不做概率权重、后台权重配置、任务冷却字段和新随机任务内容池；若后续需要长期运营精细化，可单独开新阶段处理。
- 下一阶段入口：Phase 21 建议作为阶段稳定点收口；下一阶段可转入创业知识内容、活动榜/运营配置、商会/跨服深度或新的明确系统主线。

### Phase 22 阶段总结：创业知识卡全量内容化与玩法接入

- 当前稳定点：`a300f53 feat: rotate random task categories`。
- 完成内容：创业知识库从 3 类 4 张摘要卡升级为 9 类 36 张 v1 完整知识卡，覆盖公司设立与股权、劳动用工、合同与客户、数据与隐私、广告与营销、知识产权、税务与财务、融资贷款与债务、平台运营与竞争。
- 模型变化：KnowledgeEntry 保留摘要、来源链接、采集日期、版本和免责声明，并新增经营场景、风险提示、游戏影响、行动建议、来源名称和审核状态；新增字段带默认值，支持本地已有 4 条旧知识卡无损升级。
- 接口变化：`GET /knowledge` 从只返回已解锁条目改为返回完整知识目录；未解锁知识只展示标题、分类、来源元信息和锁定提示，不泄漏完整正文；已解锁知识返回完整内容字段和解锁时间。
- 内容来源：本批知识卡逐条绑定官方来源，优先使用全国人大网、全国人大法律法规库、国家知识产权局、国家税务总局等官方入口；正文全部转写为游戏化短句，不复制大段法条。
- 前台变化：荣誉中心知识库从最多 3 张摘要升级为分类目录、解锁计数、卡片选择和完整详情；知识/合规支线页优先读取知识卡字段展示经营场景、风险提示、游戏影响、行动建议、来源、采集日期、版本和免责声明。
- 规则边界：成就仍通过旧知识卡 ID 幂等解锁关键卡；旧 ID `company-registration-basics`、`cashflow-safety-line`、`valuation-method-note`、`ai-agent-season-playbook` 保持兼容；本批不新增数值奖励、不引入新存储系统、不改随机任务/事件结算公式。
- 四插件使用证据：
  - Superpowers：先写 Phase 22 知识目录和旧 ID 解锁失败测试，再改 schema、seed、repository 和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收荣誉中心知识库分类、锁定卡和完整卡详情。
  - Build Web Apps：知识库沿用深色商务手游基线，在荣誉中心内完成分类与详情，不新增营销页式入口。
  - Game Studio：知识详情在荣誉中心和支线全覆盖内页中呈现，不强制弹窗，不遮挡主页 HUD、底部导航和专属经理流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "phase 22 knowledge|phase 14 achievements" --import tsx apps/api/test/http.test.ts`、`npm run db:push -w @wenziyouxi/api`、`npm run db:seed -w @wenziyouxi/api`，其余全量回归按本阶段收口执行。
- 遗留风险：事件结算页目前仍以知识标题和风险解释为主，尚未把每个事件结算结果直接展开完整知识卡；随机任务结果页也尚未展示完整知识卡入口。后续若继续 Phase 22，应单独做“事件/随机任务结算知识卡入口”批次。

### Phase 22 第二实施批次阶段总结：事件/随机任务知识卡入口

- 当前稳定点：`584d89f feat: expand knowledge cards`。
- 完成内容：经营事件配置和随机任务配置新增可选知识卡关联；事件列表、事件结算、随机任务列表和随机任务结算返回关联知识卡摘要、来源、版本、采集日期、免责声明和解锁状态。
- 玩法接入：事件结算会幂等解锁关联知识卡，并在结算结果中返回已解锁知识入口；随机任务结算返回关联知识摘要，帮助解释经营选择的风险/收益，但不直接解锁完整正文，避免变成刷知识奖励入口。
- 前台变化：专属经理事件详情展示相关知识卡、来源和知识库入口；已处理事件展示结算后的知识卡结果块；随机任务详情和短决策弹窗展示相关知识卡摘要，结算提示追加知识卡标题。
- 规则边界：本批不新增知识卡内容池、不改变随机任务生成、公平轮换、行动力消耗、奖励公式、经营道具修正、事件选项和知识支线奖励；只接知识卡解释入口。
- 四插件使用证据：
  - Superpowers：按 TDD 先写事件/随机任务关联知识卡失败测试，再补 schema、seed、repository 和前台入口。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收专属经理事件详情、随机任务弹窗和知识库入口。
  - Build Web Apps：前台复用既有专属经理详情、结果块和荣誉中心知识库，不新增营销页式 UI。
  - Game Studio：知识入口作为经营解释层嵌入现有短决策流，不强制弹窗、不遮挡 HUD、底部导航和专属经理主流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "linked knowledge" --import tsx apps/api/test/http.test.ts`、`npm run db:generate -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`，其余全量回归按本批收口执行。
- 遗留风险：主线任务、支线任务和更细的事件结算解释仍可继续增强；本批先完成已实现事件/随机任务链路，不扩展新任务内容和后台配置页。

### Phase 22 第三实施批次阶段总结：知识卡任务阅读闭环

- 当前稳定点：`11840ab feat: link knowledge to tasks and events`。
- 完成内容：任务配置新增可选 `knowledgeId`；任务列表返回绑定知识卡 ID；知识/合规支线从固定卡片改为读取任务配置，阅读正确卡片后推进任务进度。
- 阅读规则：知识/合规任务调用进度接口时必须传入匹配的 `knowledgeId`；未解锁知识卡返回 `KNOWLEDGE_LOCKED`，不匹配任务配置返回 `TASK_KNOWLEDGE_MISMATCH`；普通任务和没有知识卡绑定的任务保持原行为。
- 前台变化：任务页点击知识/合规支线会打开该任务绑定的知识卡；未解锁卡展示锁定提示并禁用“我已理解”；已解锁卡确认阅读后推进任务并刷新任务状态。
- 规则边界：本批不新增知识卡内容，不做 Admin 知识配置页，不改变任务奖励、事件奖励、随机任务奖励、行动力和经营道具规则；事件/随机任务知识入口仍只做解释，不直接发任务奖励。
- 四插件使用证据：
  - Superpowers：按 TDD 先写知识支线任务绑定和阅读校验失败测试，再补 schema、repository、HTTP 和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收任务页知识/合规支线、锁定提示、已解锁阅读推进和 HUD/导航遮挡。
  - Build Web Apps：前台复用既有知识支线全覆盖页、深色玻璃面板和按钮层级，不新增营销页式 UI。
  - Game Studio：知识卡阅读作为主动支线动作，不强制弹窗、不遮挡主页 HUD、底部导航和专属经理流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "knowledge side tasks progress|completes knowledge" --import tsx apps/api/test/http.test.ts`、`npm run db:generate -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`，其余全量回归按本批收口执行。
- 下一阶段入口：Phase 22 可作为知识卡玩家侧稳定点收口；后续建议转入 Admin 知识配置/审核页，或进入 Phase 23 商会与跨服深度玩法。

### Phase 22 第四实施批次阶段总结：Admin 知识配置与审核

- 当前稳定点：`116ee40 feat: bind knowledge cards to task reading`。
- 完成内容：新增 Admin 知识卡列表与审核接口，支持按关键词、分类和审核状态筛选；返回完整知识卡字段、分类列表和总数；支持更新摘要、经营场景、风险提示、游戏影响、行动建议、来源、版本、采集日期和审核状态。
- 审计规则：知识卡更新必须携带审核原因，后端写入 `admin_knowledge_update` 审计日志；不存在的知识卡返回 `KNOWLEDGE_NOT_FOUND`；未登录 Admin 访问知识接口返回 `UNAUTHORIZED`。
- Admin 前台：新增“知识审核”菜单，采用筛选区、表格和右侧编辑表单结构；可查看完整字段、切换审核状态、保存并刷新审计日志；不改玩家侧解锁策略、任务奖励、事件奖励和随机任务奖励。
- 文案规则：开发文档、seed 来源名称和测试断言已移除指定字样；后续新增可见文案、来源名称和界面说明不得再出现该字样，来源 URL 不因文案规则改写。
- 四插件使用证据：
  - Superpowers：按 TDD 先写 Admin 知识筛选、更新和审计失败测试，再实现后端与前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收知识审核菜单、筛选、详情编辑、审计反馈，并回到 `http://127.0.0.1:5173/` 确认玩家端不受影响。
  - Build Web Apps：Admin 页面沿用现有稳重后台结构，保持左侧菜单、筛选区、表格和编辑表单，不做卡片堆叠。
  - Game Studio：本批只改后台配置能力，不新增玩家弹窗层级，不遮挡玩家端 HUD、底部导航和专属经理流程。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "admin can filter and update knowledge" --import tsx apps/api/test/http.test.ts`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/admin`，其余全量回归按本批收口执行。
- 下一阶段入口：Phase 22 可作为知识系统“内容 + 玩家入口 + 任务阅读 + Admin 审核”稳定点收口；下一阶段建议进入 Phase 23 商会/跨服深度玩法，或单独做 Admin 知识批量导入/版本对比。

### Phase 23 第一实施批次阶段总结：商会任务与商会科技闭环

- 当前稳定点：`7fcc005 feat: add admin knowledge review`。
- 完成内容：商会从“加入 + 发布互助”推进为“互助贡献 + 任务领取 + 科技升级”的首个长期玩法闭环；发布互助给成员和商会各 20 贡献，当天商会任务完成后可领取额外 20 贡献。
- 数值规则：科技升级使用商会累计贡献门槛，不扣减排行榜贡献；`联合办公`、`资本人脉`、`风险复核`均为 5 级科技，门槛按 40、120、240、400、600 递进，避免首日卡死，也避免短时间满级。
- 后端接口：新增 `POST /guild/tasks/:id/claim` 和 `POST /guild/techs/:id/upgrade`；未加入商会、任务未完成、当天重复领取、科技不存在、贡献不足和满级均返回明确错误码。
- 前台表现：商会页展示任务进度、领取按钮、科技等级、加成文案和升级门槛；沿用深色商务手游页内布局，不新增弹窗层级，不遮挡 HUD、底部导航和商会主流程。
- 四插件使用证据：
  - Superpowers：按 TDD 先写商会任务领取和科技升级失败测试，再实现接口、仓储和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收商会加入、互助、任务领取、科技升级和页面无禁用字样。
  - Build Web Apps：商会页保持既有玻璃面板、金色按钮和紧凑信息密度，不改为后台式表格或营销页。
  - Game Studio：商会作为全覆盖内页承载长文本和按钮，主页 HUD 与底部导航不被新增控件遮挡。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "guild tasks can be claimed|phase 14 achievements titles knowledge and guild" --import tsx apps/api/test/http.test.ts`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`，其余全量回归按本批收口执行。
- 下一阶段入口：Phase 23 第二实施批次可继续做商会排行榜奖励和成员互助完成机制；跨服赛季化建议等商会单服闭环稳定后再进入。

### Phase 23 第二实施批次阶段总结：商会互助协作与贡献榜奖励闭环

- 当前稳定点：`985e819 feat: add guild tasks and tech upgrades`。
- 完成内容：商会互助从“发布请求拿贡献”推进为“成员协助完成请求”；新增互助完成接口、贡献榜每日结算接口、前台互助请求列表、协助按钮和贡献榜结算反馈。
- 互助规则：同商会成员可完成 `open` 状态请求；不能完成自己发布的请求；已完成请求不重复给贡献；协助者获得 15 成员贡献，商会获得 15 商会贡献。
- 任务语义：`guild-daily-help` 收口为“发布或完成一次商会协作”，任务领取仍给 20 贡献；发布互助原有 20 贡献保留，避免削弱既有日常行为。
- 榜单奖励：`POST /guild/leaderboard/settle` 按服务器日期每日每商会幂等结算一次，成员贡献前 3 名获得声望 120、80、50；不发平台币、现金或付费资源。
- 前台表现：商会页展示最近互助请求的类型、状态、发布者、创建日期；非本人 `open` 请求显示“协助”，已完成请求显示“已完成”，本人请求显示“等待成员协助”；结算结果在现有页内提示，不新增弹窗层级。
- 四插件使用证据：
  - Superpowers：按 TDD 覆盖互助完成、禁止自助、跨商会隔离、重复协助、任务领取和榜单幂等奖励。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收商会页互助列表、协助按钮、结算按钮、HUD 和底部导航。
  - Build Web Apps：商会页继续使用深色商务手游基线，信息密度紧凑，按钮和状态标签不溢出。
  - Game Studio：检查商会全覆盖内页滚动、任务流和榜单反馈不遮挡主流程。
- 下一阶段入口：Phase 23 后续可进入商会职位权限、商会公告/审核或跨服商会赛季化；不建议继续加厚互助奖励数值，当前单服协作闭环已形成稳定点。

### Phase 23 第三实施批次阶段总结：商会成员管理与职位权限

- 当前稳定点：`5026c97 feat: add guild help fulfillment rewards`。
- 完成内容：商会从“直接加入”推进为“首位创建者直接入会、后续成员申请入会、管理层审核”的成员治理闭环；新增会长、副会长、成员职位权限和成员管理入口。
- 入会规则：创建不存在的商会时，创建者成为会长；加入已有商会时只生成 `pending` 入会申请，申请人不会提前成为成员；重复申请会回到待审核状态。
- 审核规则：新增 `POST /guild/applications/:requestId/review`，会长和副会长可通过或拒绝同商会待审核申请；非成员、普通成员、跨商会或已审核申请均返回明确错误。
- 成员管理：新增 `POST /guild/members/:profileId/role` 和 `DELETE /guild/members/:profileId`；只有会长可任命普通成员为副会长或恢复成员职位；只有会长可移除普通成员；副会长不能操作会长，任何人不能移除自己。
- 前台表现：商会页展示待审核申请、通过/拒绝按钮、中文职位标签、任命副会长和移除成员按钮；继续使用页内列表与紧凑按钮，不新增弹窗层级。
- 四插件使用证据：
  - Superpowers：按 TDD 先写商会申请、审核、任命、移除和权限失败测试，再实现接口、仓储和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收申请列表、审核按钮、成员职位按钮、HUD 和底部导航。
  - Build Web Apps：商会页保持深色商务手游 UI、玻璃面板和紧凑信息密度，避免管理后台式大表格。
  - Game Studio：检查商会全覆盖内页滚动和成员管理按钮不遮挡主流程。
- 下一阶段入口：Phase 23 第四实施批次进入商会公告、协作规则与成员活跃；不在第三批继续扩展实时聊天或跨服玩法。

### Phase 23 第四实施批次阶段总结：商会公告、协作规则与成员活跃

- 当前稳定点：`0197d47 feat: add guild member permissions`。
- 完成内容：补齐商会公告、协作规则、今日活跃人数、今日协作次数和最近成员动态，形成“管理层发布规则、成员协作留痕、商会页可见反馈”的协作运营闭环。
- 后端规则：新增 `POST /guild/settings`，会长和副会长可更新商会公告与协作规则，普通成员返回 `GUILD_PERMISSION_DENIED`；公告和规则限制在 240 字以内。
- 活跃记录：发布互助、完成互助、领取商会任务、升级商会科技都会写入商会活动日志；`GET /guild` 返回今日去重活跃成员数、今日协作次数和最近成员动态。
- 前台表现：商会页头部新增今日活跃和协作统计；公告与规则面板对管理层展示可编辑文本框和保存按钮，对普通成员展示只读内容；新增最近动态列表展示成员动作和日期。
- 边界控制：不做实时聊天，不新增现金、平台币或付费资源奖励；活动统计只服务商会协作展示和后续协作项目入口。
- 四插件使用证据：
  - Superpowers：按 TDD 先写公告/规则权限、活跃统计和最近动态测试，再实现接口、仓储和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收公告保存、统计刷新、最近动态和商会主流程。
  - Build Web Apps：商会页继续使用深色商务手游 UI，统计卡、文本框和动态列表保持紧凑信息密度。
  - Game Studio：检查商会全覆盖内页滚动、HUD、底部导航和弹层不被新增面板遮挡。
- 下一阶段入口：Phase 23 第五实施批次进入商会协作项目，复用本批活动日志推动项目进度；不在第四批继续扩展实时聊天。

### Phase 23 第五实施批次阶段总结：商会协作项目

- 当前稳定点：`dbd4079 feat: add guild activity announcements`。
- 完成内容：新增商会协作项目闭环，包含联合路演、风险复核周、市场共创 3 个项目；商会页可查看项目说明、进度、目标和奖励状态。
- 进度规则：发布互助、完成互助、领取商会任务、升级商会科技都会推进商会项目进度；项目进度按商会维度累计，复用既有日常协作行为，不新增实时聊天、报名或复杂项目管理入口。
- 奖励规则：达标后通过 `POST /guild/projects/:projectId/claim` 领取声望奖励；联合路演、风险复核周、市场共创分别发放声望 60、80、100；重复领取返回 `GUILD_PROJECT_REWARD_CLAIMED`；不发现金、平台币或强付费资源。
- 前台表现：商会页新增“协作项目”面板，展示项目进度条、奖励文案和领奖按钮；未达标显示推进中，达标可领奖，已领取显示已领取；继续使用页内紧凑面板，不新增弹窗层级。
- 四插件使用证据：
  - Superpowers：按 TDD 先写项目进度、声望领奖、重复领取和非现金奖励测试，再实现接口、仓储和前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收商会页协作项目、日常协作推进、领奖反馈、HUD 和底部导航。
  - Build Web Apps：协作项目沿用深色商务手游 UI，使用短文案、进度条和紧凑按钮，避免后台表格化和营销页结构。
  - Game Studio：检查商会全覆盖内页滚动、项目卡、任务按钮和科技按钮不遮挡主流程。
- 下一阶段入口：Phase 23 第六实施批次进入跨服商会赛季化，复用现有跨服分组、跨服榜和商会贡献基础；不在第五批继续扩展现金、平台币或强付费资源奖励。

### Phase 23 第六实施批次阶段总结：跨服商会赛季化

- 当前稳定点：`bf35a13 feat: add guild collaboration projects`。
- 完成内容：新增跨服商会赛季报名、跨服商会赛季榜和幂等赛季结算；复用既有跨服分组、跨服中心和商会贡献数据，不改玩家个人跨服报名与个人跨服结算流程。
- 报名规则：`POST /cross-server/guild/register` 要求玩家已加入商会、职位为会长或副会长、商会成员数不少于 2 且当日活跃成员数不少于 2；未入会、权限不足、门槛不足和跨服分组缺失均返回明确错误码。
- 榜单规则：`GET /cross-server` 返回 `guildSeason` 和 `guildBoard`，展示商会报名状态、成员/活跃门槛、当前排名、奖励说明和跨服商会赛季榜；赛季榜按已报名商会的商会贡献排序。
- 结算规则：`POST /cross-server/guild/settle` 按服务器日幂等结算跨服商会赛季榜前 3 名，会长分别获得声望 180、120、80；不发现金、平台币或强付费资源，重复结算返回发放 0 份。
- 前台表现：跨服页新增“跨服商会赛季”面板，展示商会报名状态、成员/活跃进度、当前排名、奖励说明、赛季榜前三、报名按钮和结算按钮；沿用深色商务手游 UI，不新增弹窗层级。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未入会、权限、门槛、报名、榜单排序、结算幂等和非现金奖励测试，再实现后端与前台。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收跨服页商会赛季面板、报名按钮、结算反馈、HUD 和底部导航。
  - Build Web Apps：跨服商会赛季面板沿用现有玻璃面板、金色按钮和紧凑信息密度，不改成后台大表格。
  - Game Studio：检查跨服全覆盖内页滚动、赛季榜和结算反馈不遮挡主流程。
- 下一阶段入口：Phase 23 第七实施批次进入 Admin 商会/跨服运营后台与阶段收口。

### Phase 23 第七实施批次阶段总结：Admin 商会/跨服运营后台与阶段收口

- 当前稳定点：`769ca99 feat: add cross-server guild seasons`。
- 完成内容：新增 Admin 商会运营接口与后台页面，可查询商会列表、成员、贡献、今日活跃、科技、互助、协作项目和跨服赛季报名状态；支持按商会关键词、区服、跨服报名状态和今日活跃状态筛选。
- 运营动作：新增 Admin 手动商会贡献榜结算和跨服商会赛季结算，均复用玩家侧幂等发奖逻辑并写入审计日志；重复结算返回发放 0 条，不重复发奖。
- 奖励边界：本批不新增现金、平台币或强付费资源奖励；手动结算只触发既有贡献/声望荣誉奖励链路。
- Admin 前台：新增“商会运营”菜单，采用筛选区、表格、详情区和操作面板结构；不做卡片堆叠，不新增复杂弹窗。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未登录拦截、筛选、详情、结算幂等和非现金奖励测试，再实现后端和 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 商会运营页筛选、详情、手动结算和结果提示，并打开 `http://127.0.0.1:5173/` 做前台回归。
  - Build Web Apps：Admin 商会运营页沿用稳重运营后台结构，以筛选、表格和详情面板承载信息，不改成手游内页或营销式卡片。
  - Game Studio：检查前台商会页、跨服页、HUD 和底部导航不受 Admin 改动回归影响。
- 阶段收口：Phase 23 七个完整批次全部完成；后续不再追加 Phase 23 批次。
- 下一阶段入口：Phase 24 建议进入运营深化与长期留存，例如 Admin 细粒度运营审计、商会赛季历史归档、活动榜真实开启和赛季回顾页。

### Phase 24 第一实施批次阶段总结：活动榜真实开启与赛季回顾闭环

- 当前稳定点：`ec2defa feat: add admin guild operations`。
- 完成内容：活动榜从长期占位推进为真实运营闭环；活动开启时 `GET /leaderboards` 返回活动榜，活动未开启或已结束时隐藏常驻 Rank 入口；赛季活动页保留结束活动的回顾数据。
- 榜单规则：复用 `ActivityConfig`、`PlayerActivityState` 和活动积分，按活动积分排序展示玩家、公司、分数、排名和活动状态；榜单只在活动日期内作为进行中榜单展示。
- 结算规则：新增 Admin 手动活动榜结算接口，活动结束后可按活动结束日幂等发放前 3 名声望 120、80、50；重复结算返回发放 0 条；不发现金、平台币或强付费资源。
- Admin 前台：新增“活动运营”菜单，展示活动状态、参与人数、总积分、榜单前三、结算状态，并支持手动结算与结果提示；操作写入 Admin 审计日志。
- 前台表现：Rank 荣誉中心仅在活动进行中显示活动榜；赛季活动页展示活动榜前三、个人分数、个人名次、结算状态和活动回顾入口；不新增复杂弹窗层级。
- 四插件使用证据：
  - Superpowers：按 TDD 先写活动榜展示、回顾、Admin 未登录拦截、结算幂等和非现金奖励测试，再实现后端、Admin 和前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 活动运营页和结算提示，并打开 `http://127.0.0.1:5173/` 验收 Rank 与赛季活动页。
  - Build Web Apps：Admin 活动运营页沿用稳重后台结构，前台活动榜沿用深色商务手游信息密度，不做营销页或大弹窗。
  - Game Studio：检查 HUD、底部导航、赛季页按钮、Rank 内页滚动不被活动榜和回顾区域遮挡。
- 下一阶段入口：Phase 24 第二实施批次建议从“商会赛季历史归档”或“Admin 细粒度运营审计”中选择一个完整闭环，不继续扩展新活动内容池。

### Phase 24 第二实施批次阶段总结：Admin 细粒度运营审计与幂等重试追踪

- 当前稳定点：`01936d6 feat: add activity leaderboard recaps`。
- 完成内容：Admin 审计日志从“最近记录列表”推进为可筛选、可查看结构化明细、可追踪结算重试的运营审计闭环；不新增玩家侧玩法，不改变奖励发放规则。
- 后端接口：`GET /admin/audit-logs` 支持按动作、对象类型、对象 ID、管理员和日期范围筛选，返回 `rows`、`total` 以及可用于前台筛选的动作/对象/管理员选项；未登录 Admin 仍返回 `UNAUTHORIZED`。
- 重试追踪：排行榜、活动榜、商会榜和跨服商会赛季的 Admin 手动结算审计明细统一记录 `deliveredRewards`、`isRetry`、目标 ID、日期、原因和奖励边界，重复结算可从审计日志直接识别为发放 0 条。
- Admin 前台：审计日志页新增筛选区、表格和详情区；详情展示摘要、动作、对象、管理员、时间和结构化 JSON 明细；不新增复杂弹窗，不改玩家前台入口。
- 四插件使用证据：
  - Superpowers：按 TDD 先写审计筛选、未登录拦截和活动榜重复结算 detail 测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 审计日志筛选、详情区和重试摘要，并打开 `http://127.0.0.1:5173/` 做前台回归。
  - Build Web Apps：审计页保持稳重后台结构，以筛选、表格和右侧详情承载信息，不做卡片堆叠或营销式布局。
  - Game Studio：检查前台 Rank、赛季活动、商会页、HUD 和底部导航不受 Admin 改动回归影响。
- 下一阶段入口：Phase 24 第三实施批次建议进入“商会赛季历史归档”，沉淀单服商会榜、跨服商会赛季和成员贡献历史回顾。

### Phase 24 第三实施批次阶段总结：商会赛季历史归档

- 当前稳定点：`7fe41aa feat: add admin audit filters`。
- 完成内容：单服商会贡献榜、跨服商会赛季、成员贡献排名和结算奖励沉淀为可回顾的历史记录；本批只做只读归档与展示，不新增商会赛季玩法或奖励类型。
- 后端接口：新增 `GET /guild/history` 返回本商会最近贡献榜结算、当前成员贡献前三和已发声望奖励；新增 `GET /cross-server/guild/history` 返回本商会跨服报名状态、最终排名、榜单前三和发放数量。
- Admin 扩展：`GET /admin/guilds/:guildId` 增加 `history` 字段，展示单服商会榜与跨服商会赛季历史摘要；保持只读，不提供修改历史、成员贡献或奖励的高风险操作。
- 前台表现：商会页新增“历史荣誉”区域，跨服页新增“赛季回顾”区域；沿用深色商务手游内页结构，不新增复杂弹窗。
- 奖励边界：历史数据来自既有 `leaderboardRewardDelivery` 与商会贡献数据；不发现金、平台币或强付费资源，重复结算只显示一次历史结果。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未入会拦截、单服历史、跨服历史、Admin 摘要和非现金奖励测试，再实现接口与页面。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 验收商会历史荣誉、跨服赛季回顾，并打开 `http://127.0.0.1:5174/` 验收 Admin 商会详情历史摘要。
  - Build Web Apps：Admin 详情保持筛选、表格、详情区的稳重后台风格；前台历史展示保持信息密度和深色商务手游一致性。
  - Game Studio：检查 HUD、底部导航、商会页、跨服页和 Rank/赛季页滚动不被新增历史区域遮挡。
- 下一阶段入口：Phase 24 第四实施批次建议进入“赛季/活动运营配置可视化”，把赛季、活动、榜单、商店和结算状态做成可查看的运营配置闭环。

### Phase 24 第四实施批次阶段总结：赛季/活动运营配置可视化

- 当前稳定点：`0f6d514 feat: add guild season history`。
- 完成内容：Admin 配置清单从基础配置列表扩展为赛季/活动运营配置总览，可查看赛季、活动榜、活动商店、通行证和最近活动榜结算状态；本批只做只读可视化，不做在线编辑和内容池扩容。
- 后端接口：复用既有 `GET /admin/config-center`，新增返回 `activityShopItems`、`seasonPass`、`leaderboardSettlements`，并扩展 `seasons` 与 `activities` 的任务数、活动数、参与人数、总分、结算状态、发放数量和奖励边界字段。
- Admin 前台：配置清单页新增“赛季 / 活动运营配置总览”，以页内分组展示赛季配置、活动榜状态、活动商店、通行证、最近活动榜结算和经营剧本；不新增复杂弹窗，不改玩家前台入口。
- 奖励边界：运营配置总览只展示状态和奖励说明，不新增现金、平台币或强付费资源奖励；活动榜结算摘要展示平台币发放总额为 0，便于运营核对边界。
- 四插件使用证据：
  - Superpowers：按 TDD 先写 Admin 未登录拦截、赛季/活动/商店/通行证/结算状态测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 配置总览页，并打开 `http://127.0.0.1:5173/` 做前台回归。
  - Build Web Apps：Admin 页面保持稳重运营后台结构，以配置分组和状态文本承载信息，不做营销式页面或卡片堆叠。
  - Game Studio：检查前台 Rank、赛季活动页、商会页、HUD 和底部导航不受 Admin 配置页改动回归影响。
- 下一阶段入口：Phase 24 第五实施批次建议进入“运营配置只读巡检告警”，在不编辑配置的前提下补齐异常配置、未结算活动和奖励边界风险提示。

### Phase 24 第五实施批次阶段总结：运营配置只读巡检告警

- 当前稳定点：`5b2bbf7 feat: add operation config overview`。
- 完成内容：新增运营配置只读巡检闭环，Admin 可查看赛季、活动、活动榜、活动商店、通行证和结算记录衍生出的配置风险；本批只展示告警，不做配置编辑、自动修复或新增运营动作。
- 后端接口：新增 `GET /admin/operation-config-alerts`，返回告警摘要、筛选选项和告警列表；未登录 Admin 返回 `UNAUTHORIZED`。
- 巡检规则：覆盖已结束未结算活动、缺少活动榜 key、活动商店积分/限购异常、停用商品提示、通行证价格异常，以及活动榜奖励边界中的现金或平台币风险。
- Admin 前台：配置清单页新增“运营配置巡检告警”区域，支持按等级和类型筛选、刷新巡检、查看告警对象、说明和建议；保持稳重后台结构，不新增复杂弹窗。
- 四插件使用证据：
  - Superpowers：按 TDD 先写巡检接口未登录、摘要、未结算、奖励边界风险和结算后告警消失测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 巡检告警区域、筛选和刷新，并打开 `http://127.0.0.1:5173/` 做前台回归。
  - Build Web Apps：Admin 巡检区域沿用筛选区、摘要区和表格结构，不做卡片堆叠或营销式布局。
  - Game Studio：检查前台 Rank、赛季活动页、商会页、HUD 和底部导航不受 Admin 巡检改动回归影响。
- 下一阶段入口：Phase 24 第六实施批次建议进入“运营巡检告警处理闭环”或“活动内容池扩容”，二者择一完整实施，不混做。

### Phase 24 第六实施批次阶段总结：运营巡检告警处理闭环

- 当前稳定点：`9cc546f feat: add operation config alerts`。
- 完成内容：在只读巡检基础上补齐告警处理状态，Admin 可对当前巡检告警执行知悉、忽略和重新打开；本批只记录处理状态与审计日志，不编辑配置、不自动修复、不触发奖励发放。
- 后端接口：新增 `POST /admin/operation-config-alerts/:alertId/ack`、`POST /admin/operation-config-alerts/:alertId/ignore`、`POST /admin/operation-config-alerts/:alertId/reopen`；处理状态通过 Admin 审计日志持久化，`GET /admin/operation-config-alerts` 返回 `status`、`handledBy`、`handledAt`、`note` 和状态摘要。
- Admin 前台：配置清单页的“运营配置巡检告警”区域新增处理状态筛选、处理备注输入、知悉/忽略/重新打开按钮和处理记录列；保持表格化后台结构，不新增复杂弹窗。
- 审计与边界：处理动作统一写入 `operation_config_alert` 审计日志；不改变玩家现金、平台币、活动配置或活动榜结算状态。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未登录拦截、处理状态、审计日志和现金/平台币不变测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 巡检告警处理、状态筛选和刷新，并打开 `http://127.0.0.1:5173/` 做前台回归。
  - Build Web Apps：Admin 页面继续采用筛选区、摘要区、表格和页内提示，避免弹窗堆叠和营销式布局。
  - Game Studio：检查前台 Rank、赛季活动页、商会页、HUD 和底部导航不受 Admin 告警处理改动回归影响。
- 下一阶段入口：Phase 24 第七实施批次建议进入“活动内容池扩容”，完整扩充活动任务、活动商店兑换和活动回顾内容，不与配置编辑混做。

### Phase 24 第七实施批次阶段总结：热点/真实创业活动内容池扩容

- 当前稳定点：`90b4084 feat: add operation alert handling`。
- 完成内容：在既有活动榜、活动商店、结算和回顾闭环基础上，将赛季活动扩展为可轮换内容池；新增现金流挑战周、市场拓展冲刺、团队培养营、合规经营周、融资路演周等真实创业主题活动，支持进行中、预告和后续回顾状态。
- 后端配置：扩展 `ActivityConfig` 种子和测试内存配置，保持既有 AI 活动用于巡检告警验证；新增活动均不发现金、不发平台币，只发声望、赛季积分和荣誉称号类奖励。
- 排行与赛季页：`GET /season` 返回多活动状态，`GET /leaderboards` 在多活动同时开放时返回多张活动榜；旧活动结束后只隐藏对应活动榜，不影响其他进行中活动榜。
- 前台表现：赛季活动页从单活动展示升级为活动轮换列表、最多三张活动榜、活动回顾列表和多项活动商店兑换；Rank 荣誉中心展示多张进行中活动榜，保持深色商务手游内页结构，不新增复杂弹窗。
- Admin 可观察：配置中心自动展示扩容后的活动数量、活动状态、活动商店项和奖励边界；新增测试覆盖 Admin 可见性和新活动奖励不改变现金/平台币。
- 四插件使用证据：
  - Superpowers：按 TDD 先写多活动内容池、活动榜排序、领奖边界和 Admin 可观察测试，再实施配置、API 类型和前台展示。
  - Browser Use：需打开 `http://127.0.0.1:5173/` 与 `http://127.0.0.1:5174/` 验收前台赛季/Rank 多活动展示和 Admin 配置中心。
  - Build Web Apps：前台保持现有深色商务手游密度，Admin 继续使用稳重运营后台表格与配置区，不做营销式改版。
  - Game Studio：检查赛季页、Rank 页、HUD、底部导航和滚动区域不被多活动榜、商店项和回顾列表遮挡。
- 下一阶段入口：Phase 24 第八实施批次建议进入“活动内容池运营收口与节奏策略”，重点补活动轮换节奏说明、活动主题复用规范和运营验证清单，不与在线配置编辑混做。

### Phase 24 第八实施批次阶段总结：活动内容池运营收口与节奏策略

- 当前稳定点：`ae0fd18 feat: expand activity content pool`。
- 完成内容：在活动内容池扩容后补齐只读运营节奏视图，Admin 可查看活动轮换档期、同时开启峰值、奖励边界风险和后续活动断档提示；本批不做在线配置编辑、不新增写操作。
- 后端接口：新增 `GET /admin/activity-schedule`，走既有 Admin 鉴权，返回活动摘要、每日档期窗口、活动奖励边界和运营提示；未登录 Admin 返回 `UNAUTHORIZED`。
- 巡检规则：覆盖活动同期开启过多、无后续活动、缺少活动榜 key、现金奖励配置和平台币结算记录风险；建议活动榜奖励保持声望、称号、外观券或碎片。
- 前台表现：赛季活动页将活动列表按“当前活动 / 即将开放 / 已结束回顾”分组，保留原有报名、推进和领奖语义；不新增复杂弹窗，不改变 Rank、活动商店或结算规则。
- Admin 前台：配置清单页新增“活动轮换节奏”区域，展示节奏摘要、档期窗口、运营提示和奖励边界；继续保持稳重后台结构。
- 四插件使用证据：
  - Superpowers：按 TDD 先写 Admin 未登录、档期拥挤、奖励边界和无后续活动测试，再实现后端与页面。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 活动轮换节奏，并打开 `http://127.0.0.1:5173/` 验收赛季活动分组和前台回归。
  - Build Web Apps：Admin 页面沿用筛选、表格、配置分组的稳重运营后台结构，前台只做轻量信息层级调整。
  - Game Studio：检查赛季活动页、Rank 页、HUD、底部导航和滚动区域不被活动分组与运营信息遮挡。
- 下一阶段入口：Phase 24 第九实施批次建议进入“活动内容运营工具化前置安全校验”，先做配置草案校验与预览，不直接开放线上编辑。

### Phase 24 第九实施批次阶段总结：活动内容运营工具化前置安全校验

- 当前稳定点：`19d0f72 feat: add activity schedule operations`。
- 完成内容：新增活动配置草案只读预检闭环，运营可在 Admin 输入活动 ID、名称、档期、活动榜 key、目标分和奖励字段，提前看到阻断错误、档期拥挤提示、奖励边界风险和上线预览；本批不写入正式活动配置，不新增发布动作。
- 后端接口：新增 `POST /admin/activity-config-drafts/validate`，走既有 Admin 鉴权；未登录返回 `UNAUTHORIZED`，已登录返回 `summary`、`errors`、`warnings`、`riskLabels` 和 `preview`。
- 校验规则：覆盖活动 ID 格式与重复、名称为空、日期格式与日期区间、活动榜 key 缺失或重复、目标分非法、负数奖励、现金奖励、平台币奖励和同期开启活动过多。
- 奖励边界：草案预检只允许声望、活动积分和称号类荣誉进入正常状态；现金和平台币只作为风险标签展示，不改变玩家现金、平台币或任何正式配置。
- Admin 前台：配置清单页新增“活动草案校验”区域，使用后台表单、页内摘要、预览、错误和告警展示；不做复杂弹窗，不做保存或发布按钮。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未登录、合规草案、阻断错误、奖励风险、档期拥挤和只读不落库测试，再实现接口与 Admin 页面。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 草案校验通过与风险提示，并打开 `http://127.0.0.1:5173/` 验收前台 Rank、赛季活动页、HUD 和底部导航无回归。
  - Build Web Apps：Admin 页面继续采用稳重运营后台结构，使用筛选式表单和配置摘要区，不做卡片堆叠或营销式页面。
- Game Studio：检查新增 Admin 工具不影响前台赛季活动页、Rank 页、HUD、底部导航和滚动区域。
- 下一阶段入口：Phase 24 第十实施批次建议进入“活动配置草案持久化与审批流”，在当前只读预检稳定后，再单独做草案保存、审核、审计和发布前二次确认；不与自动发布混做。

### Phase 24 第十实施批次阶段总结：活动配置草案持久化与审批流

- 当前稳定点：`af9172a feat: add activity draft validation`。
- 完成内容：在草案只读预检稳定后，新增活动配置草案持久化和人工审批闭环；运营可保存草案、提交审核、通过或驳回，审批动作幂等记录，不发布到正式活动配置。
- 后端接口：新增 `GET /admin/activity-config-drafts`、`POST /admin/activity-config-drafts`、`POST /admin/activity-config-drafts/:draftId/submit`、`POST /admin/activity-config-drafts/:draftId/approve`、`POST /admin/activity-config-drafts/:draftId/reject`；全部走既有 Admin 鉴权。
- 数据与审计：新增 `ActivityConfigDraft` 草案表，保存草案状态、提交时间、复核时间、复核说明和操作人；保存、提交、通过、驳回统一写入 `activity_config_draft` 审计日志，重复提交或重复复核不重复写审计。
- 校验与边界：提交审核前复用第九批草案校验；现金、平台币或阻断错误草案不能进入待审核；通过/驳回不改变正式活动表、不改变玩家现金和平台币。
- Admin 前台：配置清单页“活动草案审批”区域支持校验、保存、刷新、提交审核、通过和驳回；使用表格和页内提示展示状态、校验摘要、奖励说明和审批结果，不新增复杂弹窗。
- 四插件使用证据：
  - Superpowers：按 TDD 先写保存、提交、审核、驳回、幂等、审计和资产不变测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 草案保存、提交审核、通过/驳回和结果提示，并打开 `http://127.0.0.1:5173/` 验收前台无回归。
  - Build Web Apps：Admin 页面继续采用筛选式表单、表格和页内状态提示，保持稳重运营后台结构。
  - Game Studio：检查前台赛季活动页、Rank 页、HUD、底部导航和滚动区域不被本批 Admin 改动影响。
- 下一阶段入口：Phase 24 第十一实施批次建议进入“活动草案发布前二次确认与安全发布”，只在审批流稳定后做正式发布与回滚观察，不与新活动扩容混做。

### Phase 24 第十一实施批次阶段总结：活动草案发布前二次确认与安全发布

- 当前稳定点：`337e69e feat: add activity draft approval workflow`。
- 完成内容：在草案审批流稳定后新增安全发布闭环；只有已通过草案可发布到正式活动配置，发布前必须填写二次确认说明，发布成功后草案状态变为 `published`。
- 后端接口：扩展 `POST /admin/activity-config-drafts/:draftId/publish`，走既有 Admin 鉴权；未登录返回 `UNAUTHORIZED`，未审批返回 `ACTIVITY_DRAFT_NOT_APPROVED`，阻断校验返回 `ACTIVITY_DRAFT_VALIDATION_FAILED`。
- 幂等与审计：重复发布已发布草案返回同一正式活动和 `auditLogId: null`，不重复创建活动、不重复写审计；首次发布写入 `admin_activity_draft_publish` 审计日志。
- 奖励边界：发布仍复用草案校验，现金和平台币风险不能进入发布链路；发布动作只创建活动配置，不发放任何玩家现金、平台币或奖励。
- Admin 前台：配置清单页新增发布确认输入和“安全发布”按钮；已发布草案在列表中展示“已发布”，页内提示显示发布结果和审计记录。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未登录、未审批、发布、重复发布、正式配置唯一、审计和资产不变测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收草案保存、审批、安全发布和已发布状态，并打开 `http://127.0.0.1:5173/` 验收前台无回归。
  - Build Web Apps：Admin 发布入口沿用表格、说明输入和页内提示，不新增复杂弹窗。
  - Game Studio：检查前台赛季活动页、Rank 页、HUD、底部导航和滚动区域不受发布后台改动影响。
- 下一阶段入口：Phase 24 第十二实施批次建议进入“活动配置发布后回滚观察与只读回溯”，先做发布后可观察与风险止损，不与活动内容扩容混做。

### Phase 24 第十二实施批次阶段总结：活动配置发布后回滚观察与只读回溯

- 当前稳定点：`d880425 feat: add activity draft safe publish`。
- 完成内容：在安全发布闭环后新增发布后只读观察能力，运营可查看已发布草案对应的正式活动、发布审计、参与人数、活动总分、结算状态、奖励边界和处理建议。
- 后端接口：新增 `GET /admin/activity-publish-observations`，走既有 Admin 鉴权；未登录返回 `UNAUTHORIZED`，已登录返回 `summary` 和 `rows`。
- 只读边界：本批不做回滚写操作、不编辑正式活动、不触发结算、不发放奖励；观察接口不改变玩家现金、平台币或任何配置数据。
- Admin 前台：配置清单页新增“活动发布观察”区域，展示已发布数量、奖励风险、结束未结算数量、发布审计、发布说明和运营建议；只提供刷新观察，不提供自动修复。
- 四插件使用证据：
  - Superpowers：按 TDD 先写未登录、发布观察、审计回溯、只读资产不变测试，再实现后端与 Admin 前台。
  - Browser Use：需打开 `http://127.0.0.1:5174/` 验收 Admin 活动发布观察区，并打开 `http://127.0.0.1:5173/` 验收玩家前台无回归。
  - Build Web Apps：Admin 页面继续采用稳重后台结构，用表格和页内摘要呈现观察数据，不新增复杂弹窗。
  - Game Studio：检查前台赛季活动页、Rank 页、HUD、底部导航和滚动区域不受后台只读观察影响。
- 下一阶段入口：Phase 24 第十三实施批次建议进入“活动运营发布链路收口回归与阶段总结”，做配置草案、审批、发布、观察、审计、前台活动/榜单的整体验收，不再混入新玩法扩容。

### Phase 24 第十三实施批次完整开发计划：活动运营发布链路收口回归与阶段总结

- 当前稳定点：`2f795c2 docs: require four-plugin phase checks`。
- 阶段目标：不新增活动玩法，完整收口 Phase 24 已完成的活动运营能力，确认活动配置草案、校验、审批、安全发布、发布观察、活动榜、活动回顾、活动商店、Admin 配置清单和审计日志形成稳定闭环。
- 后端收口：
  - 补全活动运营链路回归测试，覆盖草案校验、草案保存、提交审核、审批通过、驳回、安全发布、重复发布幂等、发布后观察、活动榜手动结算和审计日志追溯。
  - 保持 `/activities/:id/join|progress|claim`、活动商店、通行证、活动榜结算和玩家侧活动页语义不变。
  - 重复发布已发布草案仍返回同一正式活动，不重复写审计；重复活动榜结算仍按幂等规则返回发放 0 条。
  - 不新增现金、平台币或强付费资源奖励，不新增回滚写操作，不开放正式活动在线编辑。
- Admin 收口：
  - 配置清单页完整回归运营配置巡检告警、活动轮换节奏、活动草案校验、活动草案审批、安全发布和活动发布观察区域。
  - 保持稳重运营后台结构：筛选、表格、状态标签、页内提示和审计说明，不做复杂弹窗和卡片堆叠。
  - 发布观察只提供刷新与只读建议，不提供自动修复或配置回滚。
- 玩家前台收口：
  - 赛季活动页回归活动轮换、活动榜、活动回顾、活动商店和经营残局入口。
  - Rank 荣誉中心回归常驻榜、跨服榜和活动榜显示/隐藏。
  - HUD、右侧入口、底部导航、活动页滚动、Rank 内页滚动不被新增信息遮挡。
- 文档收口：
  - 完成后写入 Phase 24 第十三批阶段总结，标记 Phase 24 活动运营深化完成。
  - 下一阶段入口改为 Phase 25“玩家长期目标与商业化体验收口”。
- 验证要求：
  - `npm run test -w @wenziyouxi/api`
  - `npm run typecheck -w @wenziyouxi/api`
  - `npm run lint -w @wenziyouxi/api`
  - `npm run build -w @wenziyouxi/api`
  - `npm run typecheck -w @wenziyouxi/client`
  - `npm run lint -w @wenziyouxi/client`
  - `npm run build -w @wenziyouxi/client`
  - `npm run typecheck -w @wenziyouxi/admin`
  - `npm run lint -w @wenziyouxi/admin`
  - `npm run build -w @wenziyouxi/admin`
  - `git diff --check`
  - 执行既有禁用词扫描命令，确认 `apps` 和 `docs/development-plan.md` 无命中。
  - Browser Use 验收 `http://127.0.0.1:5174/` Admin 配置清单完整链路和 `http://127.0.0.1:5173/` 前台活动页、Rank 荣誉中心。
  - Game Studio 检查前台 HUD、底部导航、活动页、Rank 页和滚动区域无回归遮挡。

### Phase 24 第十三实施批次阶段总结：活动运营发布链路收口回归

- 完成内容：本批不新增活动玩法，补齐活动运营发布链路的单条完整回归测试，覆盖草案校验、保存、提交审核、审批通过、安全发布、重复发布幂等、发布观察、活动期报名/积分、活动榜结束后结算、重复结算幂等、草案审计和活动榜审计追溯。
- 后端收口：新增 `phase 24 activity operation release chain regresses publish observe settle and audit idempotently` 回归用例，确认草案发布生成的正式活动不会重复写入正式配置，重复发布不再写审计，活动榜重复结算发放 0 条，结算链路不增加现金和平台币，玩家仅获得声望类榜单奖励。
- Admin 收口：活动运营列表补充无活动空态；配置清单的活动轮换节奏按有活动的档期窗口显示空态；运营配置巡检筛选网格适配等级、处理状态、告警类型、关键字和操作按钮，保持稳重后台表格结构，不新增弹窗或自动修复。
- 玩家前台收口：本批未改玩家侧玩法语义；通过浏览器验收回归赛季活动页、Rank 荣誉中心、HUD、右侧入口、底部导航和滚动区域，确认新增后台收口信息不遮挡玩家主操作。
- 四插件执行证据：
  - Superpowers：按当前稳定点 `2f795c2` 维护最小改动范围；先补收口回归测试并执行红绿验证，红灯阶段暴露审计日期过滤断言范围过宽，收敛后确认生产 API 无需改动。
  - Browser Use：打开本地 Admin 与前台页面，验收配置清单、活动运营、赛季活动页和 Rank 荣誉中心的真实浏览器状态。
  - Build Web Apps：检查 Admin 活动运营和配置清单维持后台表格、筛选、状态标签和页内说明结构；未引入营销式卡片或复杂弹窗。
  - Game Studio：检查前台 HUD、活动页、Rank 页、底部导航和滚动容器，确认全覆盖内页滚动和主操作区域未被遮挡。
- 验证结果：`npm run test -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`、`git diff --check` 均通过；禁用词扫描无新增命中。
- Phase 24 活动运营深化完成；下一阶段入口：Phase 25 第一实施批次“长期目标中心”。

### Phase 25 完整开发计划：玩家长期目标与商业化体验收口

- 阶段目标：把主线后长期目标、商业入口、VIP、周卡/月卡、成长基金、赛季通行证、活动商店、称号、成就、商会、跨服和历史荣誉串成玩家能看懂的长期循环，提升目标清晰度、留存和付费理解。
- 设计边界：
  - 本阶段优先做聚合、展示、解释和边界巡检，不新增现金、平台币或排行榜强奖励。
  - 不新增主页一级入口，优先放在公司页、Rank 荣誉中心、通行证页或现有全覆盖内页中。
  - 不改真实支付回调，不改平台币购买、VIP 经验、通行证、活动商店和排行榜结算语义。

#### Phase 25 第一实施批次：长期目标中心

- 后端新增或扩展聚合能力，推荐新增 `GET /long-term-goals`，聚合每日目标、周目标、赛季目标、活动目标、商会目标、跨服目标、成就称号目标和满级成长目标。
- 前台新增“长期目标”区域，优先接入公司页或 Rank 荣誉中心，不占用主页右侧入口。
- 展示内容：
  - 今日建议：处理待办、查看财务、完成每日任务、参与活动。
  - 本周目标：经营报告、活动榜、商会贡献、项目/产品推进。
  - 赛季目标：通行证进度、活动积分、活动商店、赛季称号。
  - 长期目标：公司等级、满级宝箱、成就、称号、跨服、商会历史。
- 验收标准：玩家进入一个区域即可看清“今天做什么、本周追什么、赛季争什么、长期收集什么”。

##### Phase 25 第一实施批次总结：长期目标中心

- 完成内容：新增 `GET /long-term-goals` 只读聚合接口，复用每日任务、公司成长、赛季活动、活动榜、商会、跨服、称号和成就数据，返回今日建议、本周目标、赛季目标和长期目标四组内容。
- 前台接入：在 Rank 荣誉中心顶部新增“长期目标”区域，不新增主页一级入口，不挤占 HUD、右侧入口或底部导航。
- 边界检查：本批只做聚合和展示，不修改通行证购买、活动商店购买、VIP 经验、支付回调、排行榜结算、活动榜结算和 Admin 活动运营链路；不新增现金、平台币或排行榜强奖励。
- 四插件检查：
  - Superpowers：按 Phase 25 第一批计划执行，使用 TDD 先写 `/long-term-goals` 回归测试，红灯为 404，绿灯后保留测试。
  - Browser Use：本地浏览器验收 Rank 荣誉中心长期目标区域、活动页、通行证页和商会入口不回归。
  - Build Web Apps：长期目标沿用深色商务手游 UI、金色状态强调、可滚动内页结构，移动端以单列展示。
  - Game Studio：长期目标仅在 Rank 全覆盖内页展示，不遮挡主页 HUD、资源栏、主线任务条、底部导航和弹窗层级。
- 验证命令：`npm run test -w @wenziyouxi/api -- --test-name-pattern="phase 25 long-term goals"`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`。

#### Phase 25 第二实施批次：商业入口体验收口

- 梳理并固化入口职责：
  - 商业：普通商城、礼包、经营道具、活动材料补给。
  - 特权：周卡、月卡、成长基金。
  - 通行证：赛季通行证、赛季奖励线、赛季任务承接。
  - VIP：头像身份入口和权益说明，不恢复为右侧独立按钮。
  - 背包：已获得道具、礼包、招募券和经营道具使用入口。
- 前台文案统一为“缓解经营压力、提高效率、获得更多选择”，不写“买了就赢”。
- 商业、特权、通行证、背包之间增加清晰跳转提示，但不新增复杂弹窗。
- 验收标准：新玩家能理解每个付费入口的区别，老玩家能快速找到月卡、成长基金、通行证、活动商店和经营道具。

##### Phase 25 第二实施批次总结：商业入口体验收口

- 完成内容：在商业、特权、通行证、VIP 和背包现有全覆盖页补充入口导航说明，明确普通商品、周卡/月卡/成长基金、赛季通行证、VIP 权益和背包道具的职责边界。
- 前台接入：复用现有 `openHomePanel`、`shopCenter`、`vipCenter`、`seasonCenter` 和 `inventoryCenter`，不新增主页一级入口、不新增弹窗、不新增后端接口。
- 边界检查：本批只调整前台说明与跳转提示，不修改平台币购买、VIP 经验、通行证购买、活动商店、支付预留、排行榜结算和 Admin 配置链路。
- 四插件检查：
  - Superpowers：按 TDD 先写商业入口与 VIP 入口文案静态检查，红灯缺失对应文案，绿灯后保留检查命令作为回归证据。
  - Browser Use：本地浏览器验收商业、特权、通行证、VIP、背包互跳，活动、排行、商会入口不回归。
  - Build Web Apps：入口说明沿用现有深色商务手游 UI、紧凑卡片和金色轻按钮，不改变页面主视觉层级。
  - Game Studio：说明卡只在全覆盖内页滚动区展示，不遮挡主页 HUD、资源条、主线任务条和底部导航。
- 验证命令：商业入口文案静态检查、VIP 入口文案静态检查、`npm run typecheck -w @wenziyouxi/client`。

#### Phase 25 第三实施批次：付费价值边界检查

- 后端补充商业化边界测试：
  - 平台币消费计入 VIP 经验。
  - 后台发放平台币不自动计入 VIP 经验。
  - 通行证、月卡、成长基金不直接改变排行榜结算奖励。
  - 活动商店不大量产出平台币。
  - 经营道具只缓解风险，不直接免除经营判断。
- Admin 推荐新增 `GET /admin/monetization-boundaries`，只读展示平台币来源、平台币消耗、VIP 经验来源、付费商品奖励类型和风险项。
- 验收标准：商业化配置不会破坏现金、平台币、VIP、排行榜和活动榜边界。

##### Phase 25 第三实施批次总结：付费价值边界检查

- 完成内容：新增 `GET /admin/monetization-boundaries` 只读接口，集中展示平台币来源、平台币消耗、VIP 经验来源、付费商品奖励类型、通行证边界、活动商店边界和风险项。
- Admin 接入：在“配置与排行榜”页新增“付费价值边界”面板，复用现有后台表格、配置网格和深色稳重结构，不新增写入动作、不新增审批入口。
- 边界检查：本批只读检查商业化配置，不修改平台币购买、后台发币、VIP 经验调整、通行证购买、活动商店购买、支付预留、排行榜结算和活动榜结算链路。
- 四插件检查：
  - Superpowers：按 TDD 先写 `/admin/monetization-boundaries` 回归测试，红灯为 404，绿灯后保留测试。
  - Browser Use：本地浏览器验收 Admin 付费价值边界面板、前台活动页、Rank 荣誉中心、HUD 和底部导航不回归。
  - Build Web Apps：Admin 面板沿用现有后台信息层级、表格和配置网格，保持紧凑、可扫描、移动端可滚动。
  - Game Studio：本批不新增前台玩法入口；验收重点为前台 HUD、底部导航、全覆盖内页滚动区和主操作不被遮挡。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "phase 25 admin monetization boundaries" --import tsx apps/api/test/http.test.ts`、商业化边界相关 API 回归、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/admin`、`npm run build -w @wenziyouxi/admin`。

#### Phase 25 第四实施批次：长期荣誉展示收口

- 前台统一展示玩家长期荣誉：
  - 当前装备称号、已获得称号、成就进度、赛季荣誉、活动回顾、商会历史、跨服历史。
  - Rank 荣誉中心增加“我的荣誉”区域；活动、商会、跨服页保留各自历史区域，但避免重复堆满信息。
- 后端优先复用现有称号、成就、活动回顾、商会历史、跨服历史接口；只在聚合确有必要时扩展轻量返回。
- 验收标准：玩家能看到自己过去做过什么、赢过什么、还差什么。

##### Phase 25 第四实施批次总结：长期荣誉展示收口

- 完成内容：Rank 荣誉中心新增“我的荣誉”区域，聚合当前装备称号、已获得称号、成就进度、赛季荣誉、活动回顾、商会历史和跨服历史。
- 前台接入：复用已加载的 `titleCenter`、`achievements`、`seasonCenter.activityRecaps`、`guildHistory` 和 `crossServerGuildHistory`，不新增主页入口、不新增弹窗、不新增后端接口。
- 边界检查：本批只做只读荣誉展示，不修改称号装备、成就领取、赛季任务、活动榜、商会结算、跨服结算和长期目标接口行为。
- 四插件检查：
  - Superpowers：按 TDD 先写 Rank 荣誉中心文案静态回归，红灯缺失“我的荣誉”，绿灯后保留测试。
  - Browser Use：本地浏览器验收 Rank 荣誉中心“我的荣誉”、长期目标、活动页、HUD 和底部导航不回归。
  - Build Web Apps：新区域沿用现有深色商务手游 UI、玻璃面板、紧凑统计卡和滚动区信息层级。
  - Game Studio：新区域仅在 Rank 全覆盖内页滚动区展示，不遮挡主页 HUD、资源条、主线任务条和底部导航。
- 验证命令：`node --test apps/client/test/phase25-honor-center.test.mjs`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`。

#### Phase 25 第五实施批次：阶段收口

- 全量回归长期目标、商业入口、通行证、活动商店、VIP、特权、背包、称号、成就、商会和跨服历史。
- Browser Use 验收玩家前台入口路径和 Admin 商业化边界摘要。
- Game Studio 检查主页入口密度不增加、HUD 不遮挡、商业/特权/通行证/背包路径清晰。
- 文档写入 Phase 25 总结，下一阶段入口改为 Phase 26“经营时间流动与在线/离线脉冲”。

##### Phase 25 第五实施批次总结：阶段收口

- 完成内容：Phase 25 五个实施批次完成收口；长期目标、商业入口、付费价值边界、长期荣誉展示和阶段回归均保留在现有入口内，不新增玩法、不新增主页一级入口。
- 回归范围：长期目标、商业入口、通行证、活动商店、VIP、特权、背包、称号、成就、商会历史、跨服历史、Admin 付费价值边界和活动运营相关只读链路完成阶段检查。
- 边界检查：本批只做验证和文档总结，不修改支付回调、平台币购买、后台发币、VIP 经验、通行证购买、活动商店购买、排行榜结算、活动榜结算和商会/跨服结算语义。
- 四插件检查：
  - Superpowers：以 `08a7f17 feat: add long-term honor summary` 为当前稳定点，按阶段收口执行最小回归；两路只读智能体分别复核 API/Admin 与前台/Admin 浏览器验收范围，结论均为本批无需新增代码。
  - Browser Use：本地浏览器验收前台活动、排行、商业、特权、通行证、背包入口，以及 Admin 配置清单“付费价值边界”；浏览器 console 无 error。
  - Build Web Apps：前台继续沿用深色商务手游 UI、紧凑全覆盖内页和稳重后台结构；Admin 配置清单保持表格与信息块可扫描，不新增营销式页面。
  - Game Studio：主页 HUD、资源条、左右入口、主线任务条和底部导航密度未增加；长期目标和我的荣誉仍位于 Rank 全覆盖滚动区，不遮挡主操作。
- 验证命令：`node --test apps/client/test/phase25-honor-center.test.mjs`、`node --test --test-reporter spec --test-name-pattern "phase 25 long-term goals|phase 25 admin monetization boundaries|phase 19 runs season activity pass rewards|lists wallet and buys shop products|platform coin spending upgrades VIP|admin coin grants do not increase VIP|phase 24 guild season history archives|phase 14 achievements titles knowledge and guild basics" --import tsx apps/api/test/http.test.ts`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run typecheck -w @wenziyouxi/admin`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/admin`。
- Phase 25 玩家长期目标与商业化体验收口完成；下一阶段入口：Phase 26 第一实施批次“经营时钟与懒同步基础”。

### Phase 26 完整开发计划：经营时间流动与在线/离线脉冲

- 阶段目标：解决文字模拟经营游戏“数据只有点击任务才变化”的问题。让在线与离线都产生可信的小幅经营变化、简报和待办，同时避免挂机暴富和经济膨胀。
- 设计边界：
  - 所有可信结算使用后端服务器时间，不依赖前端计时。
  - 经营时间流动是“感知变化、风险解释、待办生成”，不是主要资源来源。
  - 不新增平台币自然产出，不直接放大排行榜奖励，不让离线收益成为主要现金来源。
  - 行动力继续沿用现有恢复规则，只把恢复结果纳入简报展示。

#### Phase 26 第一实施批次：经营时钟与懒同步基础

- 后端建立经营时钟，推荐扩展 `GET /company/status` 懒同步，减少新接口扩散。
- 推荐新增字段：
  - `PlayerProfile.businessClockSyncedAt`
  - `PlayerProfile.lastBusinessPulseSummaryJson`
- 结算规则：
  - 5 分钟内重复请求不结算。
  - 在线每 5 分钟最多 1 个经营 tick。
  - 离线最多累计 8 小时。
  - 现金按净现金流折算小额变化，并设置单次和累计上限。
  - 估值、员工满意度、客户满意度、风险状态只做轻微波动。
  - 平台币、VIP 经验、排行榜奖励不变。
- 前台主页资源条显示轻提示，例如“离线经营 +3.2万”“经营波动 -0.8万”；财务页展示最近经营脉冲。
- 验收标准：经营数据有变化感，但不会替代项目、产品、任务和事件结算。

##### Phase 26 第一实施批次总结：经营时钟与懒同步基础

- 完成内容：扩展 `GET /company/status` 懒同步经营时钟，新增 `businessClockSyncedAt` 和 `lastBusinessPulseSummaryJson` 持久字段，返回最近经营脉冲摘要。
- 结算边界：首次访问只建立时钟；5 分钟内重复访问不结算；超过 5 分钟按 5 分钟 tick 同步，离线最多累计 8 小时；现金、估值、员工满意度和客户满意度只做轻量变化；平台币、VIP 经验和榜单奖励始终为 0。
- 前台接入：主页 HUD 下方增加短提示，点击进入现有财务页；财务页新增“最近经营脉冲”只读卡片，展示同步分钟、现金变化、估值变化和离线经营分钟，不新增主页一级入口、不新增弹窗。
- 边界检查：本批不修改月结/日结按钮语义，不改变支付回调、平台币购买、后台发币、VIP 经验、通行证购买、活动商店、排行榜结算、活动榜结算和商会/跨服结算。
- 四插件检查：
  - Superpowers：按 TDD 先写 `/company/status` 经营时钟红灯测试和前台文案红灯检查，确认失败后实现最小代码并转绿。
  - Browser Use：本地浏览器验收主页 HUD、资源条、任务条、底部导航和财务页“最近经营脉冲”；API 重启后 fresh console error 为 0。
  - Build Web Apps：前台沿用深色商务手游 UI、资源轻提示和财务页玻璃面板，不新增营销式页面和复杂层级。
  - Game Studio：经营时钟提示不遮挡左右入口、主线任务条、底部导航和财务页主操作；全覆盖内页仍可滚动。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "phase 26 company status|loads and settles company finance|platform coin spending upgrades VIP|phase 25 long-term goals|phase 25 admin monetization boundaries" --import tsx apps/api/test/http.test.ts`、`node --test apps/client/test/phase26-business-clock.test.mjs apps/client/test/phase25-honor-center.test.mjs`、`npm run db:generate -w @wenziyouxi/api`、`npm run db:push -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`。
- 下一批入口：Phase 26 第二实施批次“夜间经营简报”。

#### Phase 26 第二实施批次：夜间经营简报

- 离线超过 30 分钟生成夜间经营简报。
- 简报内容：
  - 离线时长、行动力恢复、现金变化、估值变化、满意度变化、风险提示、新待办数量和建议动作。
- 前台使用主页提示或财务页入口，不新增复杂弹窗层级。
- 重复打开简报不重复结算。
- 验收标准：玩家回归时知道公司在离线期间发生了什么，以及下一步应处理什么。

##### Phase 26 第二实施批次总结：夜间经营简报

- 完成内容：扩展 `GET /company/status` 的 `businessClock.nightBriefing`，离线结算达到 30 分钟后返回夜间经营简报，包含离线时长、行动力恢复展示值、现金变化、估值变化、员工/客户满意度变化、风险提示、新待办数量和建议动作。
- 幂等边界：夜间简报复用经营时钟懒同步结果，不新增领取接口；重复打开 5 分钟冷却内不重复结算现金、估值和满意度，平台币、VIP 经验和榜单奖励继续保持 0。
- 前台接入：只在现有财务页“最近经营脉冲”卡片下展示“夜间经营简报”，不新增主页入口、不新增回归弹窗、不改变 HUD 和底部导航结构。
- 四插件检查：
  - Superpowers：按 TDD 先补 API 夜间简报红灯测试和前台简报文案红灯检查，再做最小实现并验证转绿。
  - Browser Use：本地浏览器验收前台主页、财务页“最近经营脉冲”、底部导航、Admin 首页和 API health；前台/Admin fresh console error 为 0。
  - Build Web Apps：简报沿用深色商务手游财务面板层级，作为只读信息块承接，不做营销式弹窗和额外入口。
  - Game Studio：夜间简报不占用 HUD，不遮挡主页任务条、左右入口、底部导航和财务页滚动区域。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "phase 26 company status|loads and settles company finance|platform coin spending upgrades VIP|phase 25 long-term goals|phase 25 admin monetization boundaries" --import tsx apps/api/test/http.test.ts`、`node --test apps/client/test/phase26-business-clock.test.mjs apps/client/test/phase25-honor-center.test.mjs`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 下一批入口：Phase 26 第三实施批次“经营脉冲生成专属经理待办”。

#### Phase 26 第三实施批次：经营脉冲生成专属经理待办

- 高负债、负现金流、员工满意度下降、客户满意度下降、风险状态恶化时，有概率生成专属经理待办。
- 复用现有随机经营任务和每日上限；商城、领奖、通行证购买等操作中不强插弹窗。
- 生成待办不消耗行动力，玩家处理时仍走既有随机任务选择与道具修正规则。
- 验收标准：经营脉冲能转化为可处理事项，而不是只在资源条上闪一下。

##### Phase 26 第三实施批次总结：经营脉冲生成专属经理待办

- 完成内容：`GET /company/status` 在经营时钟产生有效结算时，会按风险原因尝试生成当天专属经理随机待办；高负债优先生成 `random-loan-rate-review`，现金流/风险状态异常生成 `random-cashflow-warning`，满意度下降保留员工/市场类映射。
- 幂等边界：待办复用 `PlayerRandomTask`、`RandomTaskConfig` 和 `profileId + configId + dailyDate` 唯一键；重复打开公司状态不会重复生成，同日任务达到随机任务每日上限时不突破上限。
- 玩法边界：生成待办本身不消耗行动力，不发放奖励，不影响平台币、VIP 经验、榜单奖励、商城、领奖、通行证购买、活动商店和随机任务处理结算；玩家处理时仍走既有专属经理随机任务详情、短决策弹窗和经营道具修正规则。
- 前台接入：不新增 HUD 条、主页入口或强制弹窗；生成后的待办自然出现在现有“专属经理”随机任务列表中，财务页夜间简报继续只显示“新待办”和“建议动作”。
- 四插件检查：
  - Superpowers：按 TDD 先补经营脉冲生成待办红灯测试，确认失败于未生成 `random-loan-rate-review` 后再实现最小代码并转绿。
  - Browser Use：本地浏览器验收前台专属经理入口、随机任务列表/详情/弹窗、Admin 首页和 API health。
  - Build Web Apps：本批不新增前台视觉体系，继续复用深色商务手游专属经理全覆盖内页、随机任务列表和短决策弹窗。
  - Game Studio：经营脉冲待办只作为主动进入专属经理后的可处理事项，不遮挡主页 HUD、任务条、底部导航、财务页和随机任务弹窗滚动区域。
- 验证命令：`node --test --test-reporter spec --test-name-pattern "phase 26 business clock creates manager todo|phase 26 company status" --import tsx apps/api/test/http.test.ts`、`node --test --test-reporter spec --test-name-pattern "phase 26 company status|phase 26 business clock creates manager todo|regular random task generation|season pass adds one daily season random task|uses finance advisor card|lists funding and loan random tasks|lists loan products and applies cashflow loans" --import tsx apps/api/test/http.test.ts`、`node --test apps/client/test/phase26-business-clock.test.mjs apps/client/test/phase25-honor-center.test.mjs`、`npm run typecheck -w @wenziyouxi/api`、`npm run typecheck -w @wenziyouxi/client`、`npm run lint -w @wenziyouxi/api`、`npm run lint -w @wenziyouxi/client`、`npm run build -w @wenziyouxi/api`、`npm run build -w @wenziyouxi/client`、`git diff --check`。
- 下一批入口：Phase 26 第四实施批次“Admin 经营时钟观测”。

#### Phase 26 第四实施批次：Admin 经营时钟观测

- 新增 Admin 只读观察，推荐接口 `GET /admin/business-clock-observations`。
- 展示内容：
  - 玩家最近同步时间、离线时长分布、现金变化分布、风险脉冲数量、待办生成数量和异常同步提示。
- Admin 页面保持表格、筛选和页内摘要结构，不新增自动修复。
- 验收标准：运营能判断经营时钟是否正常，不需要直接改玩家资产。

#### Phase 26 第五实施批次：阶段收口

- 全量验证经营时钟不破坏平台币、VIP、排行榜、活动榜、项目结算、财务月报和商会/跨服结算。
- Browser Use 验收首页资源轻提示、财务页简报、专属经理待办、活动页和 Rank。
- Game Studio 检查 HUD、底部导航、主页任务条、全覆盖内页滚动无回归。
- 文档写入 Phase 26 总结，下一阶段入口改为 Phase 27“商业化稳定、数据化调优与上线前质量收口”。

### Phase 27 完整开发计划：商业化稳定、数据化调优与上线前质量收口

- 阶段目标：基于 Phase 23-26 新增的商会、跨服、活动运营、长期目标、商业体验和经营时钟，重新做一次上线前质量与商业化稳定收口。Phase 17/18 已完成早期基础，但新系统已扩展，需要后置收口。
- 设计边界：
  - 不接真实支付回调，不做真实部署，不改生产配置。
  - 不新增高风险资产编辑，不自动修复玩家资产。
  - 以数据可观察、风险可发现、主流程可回归为目标。

#### Phase 27 第一实施批次：商业化数据看板补齐

- Admin 数据看板扩展：
  - 长期目标点击、商业入口点击、周卡/月卡/成长基金/通行证/活动商店转化、经营道具使用、夜间简报打开率、经营时钟待办处理率。
- 前台埋点只接关键入口，不阻塞主流程；不读取浏览器历史、剪贴板或本地敏感活动记录。
- 推荐接口扩展或新增 `GET /admin/monetization-analytics`。
- 验收标准：运营能看到付费入口和长期目标是否被玩家理解与使用。

#### Phase 27 第二实施批次：经济异常巡检

- Admin 新增只读经济巡检，推荐接口 `GET /admin/economy-alerts`。
- 巡检内容：
  - 平台币异常增长、VIP 经验异常、离线现金异常、活动榜奖励边界风险、商会/跨服重复结算风险、经营时钟同步频率异常。
- 不做自动修复，不做资产编辑。
- 验收标准：异常能被识别，正常配置不误报严重风险，巡检不改变玩家资产。

#### Phase 27 第三实施批次：玩家主流程上线前回归

- 回归路径：
  - 注册、登录、恢复登录、创建公司、首页 HUD、财务、员工、业务、市场、商会、跨服、活动、Rank、商业、特权、通行证、背包、专属经理。
- Game Studio 验收重点：
  - 竖屏主流程不被遮挡，全覆盖内页滚动正常，底部导航稳定，右侧入口不拥挤，数字变化提示不干扰主线任务条。
- 验收标准：玩家主流程可连续跑通，不出现入口遮挡、主操作不可点或关键状态不同步。

#### Phase 27 第四实施批次：Admin 主流程上线前回归

- 回归路径：
  - Admin 登录、数据看板、玩家查询、平台币/VIP、称号/补偿、跨服分组、商会运营、活动运营、配置清单、知识审核、审计日志、长期目标/商业边界/经营时钟/经济巡检区域。
- 验收标准：
  - 筛选、表格、详情、刷新、手动结算、审计入口稳定；不新增复杂弹窗；不把 Admin 做成卡片堆叠页。

#### Phase 27 第五实施批次：Readiness 与生产前质量收口

- 后端检查：
  - `/health`、`/readiness`、MySQL/Redis 依赖、关键接口错误码、限流、鉴权和审计。
- 构建检查：
  - API/Admin/Client 全量 test/typecheck/lint/build。
- 文档：
  - 更新上线前检查清单、当前稳定点和 Phase 27 完成总结。
- 验收标准：仓库达到本地商业测试上线前质量收口状态，但不执行真实部署。

### Phase 24-27 全局验证要求

- Phase 24 第十三批、Phase 25、Phase 26 和 Phase 27 的每个实施批次都必须使用四插件完成开发与检查，并在阶段总结中写明证据：
  - Superpowers：阶段开始前确认目标、成功标准、当前稳定点、最小改动范围；实现时优先按 TDD 或明确执行计划推进；提交前做验证前检查。
  - Browser Use：每批完成后打开本地前台、Admin 或 API 健康入口做真实浏览器验收；纯后端批次也要至少确认主页、核心入口和 Console 无关键回归。
  - Build Web Apps：涉及前台或 Admin 页面时检查深色商务手游 UI、稳重后台结构、文案层级、按钮状态、移动端适配和页面密度；不做营销式改版或卡片堆叠。
  - Game Studio：检查玩法入口、HUD、底部导航、全覆盖内页、滚动区域、弹窗层级、奖励反馈和主操作按钮不遮挡、不打断、不破坏游戏节奏。
- 每个实施批次稳定前默认执行：
  - `npm run test -w @wenziyouxi/api`
  - `npm run typecheck -w @wenziyouxi/api`
  - `npm run lint -w @wenziyouxi/api`
  - `npm run build -w @wenziyouxi/api`
  - `npm run typecheck -w @wenziyouxi/client`
  - `npm run lint -w @wenziyouxi/client`
  - `npm run build -w @wenziyouxi/client`
  - `npm run typecheck -w @wenziyouxi/admin`
  - `npm run lint -w @wenziyouxi/admin`
  - `npm run build -w @wenziyouxi/admin`
  - `git diff --check`
  - 执行既有禁用词扫描命令，确认 `apps` 和 `docs/development-plan.md` 无命中。
- Browser Use：
  - 玩家前台：`http://127.0.0.1:5173/`
  - Admin：`http://127.0.0.1:5174/`
  - API 健康：`http://127.0.0.1:3001/health`
  - 上线收口批次检查 readiness：`http://127.0.0.1:3001/readiness`
- Game Studio：
  - 每批按影响范围检查 HUD、底部导航、主页入口、全覆盖内页、滚动区域、弹窗层级和主操作按钮。

### Phase 23 后续完整开发计划：商会与跨服深度闭环

- 阶段总目标：Phase 23 共 7 个完整批次，不做骨架；每批必须包含后端规则、前台入口、测试、Browser/Game QA 和文档同步，稳定后再提交。
- 当前进度：第一批“商会任务与商会科技闭环”已在 `985e819 feat: add guild tasks and tech upgrades` 成为稳定点；第二批“商会互助协作与贡献榜奖励闭环”已在 `5026c97 feat: add guild help fulfillment rewards` 成为稳定点；第三批“商会成员管理与职位权限”已在 `0197d47 feat: add guild member permissions` 成为稳定点；第四批“商会公告、协作规则与成员活跃”已在 `dbd4079 feat: add guild activity announcements` 成为稳定点；第五批“商会协作项目”已在 `bf35a13 feat: add guild collaboration projects` 成为稳定点；第六批“跨服商会赛季化”已在 `769ca99 feat: add cross-server guild seasons` 成为稳定点；第七批“Admin 商会/跨服运营后台与阶段收口”已完成，等待本轮提交形成最终稳定点。
- 第三实施批次：商会成员管理与职位权限，已完成。
- 第四实施批次：商会公告、协作规则与成员活跃，已完成。
- 第五实施批次：商会协作项目，已完成。
- 第六实施批次：跨服商会赛季化，已完成。
- 第七实施批次：Admin 商会/跨服运营后台与阶段收口，已完成。
- 数值边界：商会奖励以贡献、声望、称号、外观券或碎片为主；不引入现金、平台币和可直接付费拉开差距的资源；跨服奖励偏荣誉展示和长期目标。
- UI 边界：商会仍作为全覆盖内页承载，不新增复杂弹窗层级；移动竖屏必须可滚动，HUD、底部导航、任务流和专属经理流程不被遮挡。
- 下一阶段入口：Phase 24 建议进入运营深化与长期留存，优先从 Admin 细粒度运营审计、商会赛季历史归档、活动榜真实开启和赛季回顾页中选择一个完整批次。

## 12. 创业知识内容规则

- 知识内容在开发和运营配置时必须联网检索，不凭空编写关键法律常识。
- 优先使用全国人大网、政府网、国务院部门官网、国家市场监督管理总局、国家税务总局、国家网信办等官方来源。
- 可使用《公司法》《劳动合同法》《民法典合同编》《个人信息保护法》《广告法》《商标法》《著作权法》《税收征收管理法》等作为知识来源范围。
- 每条知识卡必须记录来源名称、来源链接、采集日期、内容版本和审核状态。
- 知识卡只做游戏科普和风险提示，不构成现实法律建议。
- 禁止复制大段法条进游戏正文，正文应转写成短句、场景化、可扫读的解释。
- 事件结算页必须把知识点和经营结果绑定，让玩家理解“为什么这个选择会带来这个风险或收益”。

## 13. 关键假设

- 布局固定采用《少年三国志》式竖版商业手游结构。
- 已确认采用“稳重运营后台 + 手游式玩家入口”方案：前台和 Admin 都必须独立登录后才能进入对应系统，Admin 采用稳重运营后台结构，不采用卡片模块堆积。
- 游戏核心是 H5 + 文字事件 + 真实模拟经营。
- 主数据库使用 MySQL。
- Redis 用于缓存、排行榜、锁、会话和活动状态。
- 注册登录、选区服、选头像、命名、创建公司是正式流程。
- 主线、支线、每日任务、事件、平台币、VIP、排行榜、称号、成就、创业知识库都纳入第一版完整设计。
- 主线完成后通过赛季、限时活动、经营残局、商会、跨服、成就、知识库和外观收集维持长期留存。
- 第一版主要以平台币商业化为主，外部支付接口只预留。
- 平台币消费计入 VIP 经验。
- 后台发放平台币不默认计入 VIP 经验。
- 常驻排行榜保持少而清晰，活动榜只在活动开启时展示。
- 备份和回滚依赖宝塔面板，不在项目中自建。
